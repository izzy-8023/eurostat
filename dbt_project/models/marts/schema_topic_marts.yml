version: 2
models:
- name: mart_causes_of_death___infant_mortality
  description: 'Topic mart for ''Causes of death - infant mortality''.

    Consolidates data from 4 Eurostat fact views related to this topic: HLTH_CD_AINFO,
    HLTH_CD_AINFR, HLTH_CD_YINFO, HLTH_CD_YINFR.

    Provides a unified view with harmonized dimension foreign keys and measures.

    Columns include: various conformed dimension foreign keys, value, status_code,
    status_label, linear_index, and source_dataset_code.

    Materialized as a table.'
  columns:
  - name: unit_code
    description: Foreign key to dim_unit.unit_code, linking to unit dimension.
    tests:
    - relationships:
        to: dim_unit
        field: unit_code
  - name: age_code
    description: Foreign key to dim_age.age_code, linking to age dimension.
    tests:
    - relationships:
        to: dim_age
        field: age_code
  - name: icd10_code
    description: Foreign key to dim_icd10.icd10_code, linking to icd10 dimension.
    tests:
    - relationships:
        to: dim_icd10
        field: icd10_code
  - name: time_code
    description: Foreign key to dim_time.time_code, linking to time dimension.
    tests:
    - relationships:
        to: dim_time
        field: time_code
  - name: geo_code
    description: Foreign key to dim_geo.geo_code, linking to geo dimension.
    tests:
    - relationships:
        to: dim_geo
        field: geo_code
  - name: freq_code
    description: Foreign key to dim_freq.freq_code, linking to freq dimension.
    tests:
    - relationships:
        to: dim_freq
        field: freq_code
  - name: resid_code
    description: Foreign key to dim_resid.resid_code, linking to resid dimension.
    tests:
    - relationships:
        to: dim_resid
        field: resid_code
  - name: sex_code
    description: Foreign key to dim_sex.sex_code, linking to sex dimension.
    tests:
    - relationships:
        to: dim_sex
        field: sex_code
  - name: isced11_code
    description: Foreign key to dim_isced11.isced11_code, linking to isced11 dimension.
    tests:
    - relationships:
        to: dim_isced11
        field: isced11_code
  - name: lev_limit_code
    description: Foreign key to dim_lev_limit.lev_limit_code, linking to lev_limit
      dimension.
    tests:
    - relationships:
        to: dim_lev_limit
        field: lev_limit_code
  - name: hlth_pb_code
    description: Foreign key to dim_hlth_pb.hlth_pb_code, linking to hlth_pb dimension.
    tests:
    - relationships:
        to: dim_hlth_pb
        field: hlth_pb_code
  - name: deg_urb_code
    description: Foreign key to dim_deg_urb.deg_urb_code, linking to deg_urb dimension.
    tests:
    - relationships:
        to: dim_deg_urb
        field: deg_urb_code
  - name: assist_code
    description: Foreign key to dim_assist.assist_code, linking to assist dimension.
    tests:
    - relationships:
        to: dim_assist
        field: assist_code
  - name: smoking_code
    description: Foreign key to dim_smoking.smoking_code, linking to smoking dimension.
    tests:
    - relationships:
        to: dim_smoking
        field: smoking_code
  - name: quant_inc_code
    description: Foreign key to dim_quant_inc.quant_inc_code, linking to quant_inc
      dimension.
    tests:
    - relationships:
        to: dim_quant_inc
        field: quant_inc_code
  - name: n_portion_code
    description: Foreign key to dim_n_portion.n_portion_code, linking to n_portion
      dimension.
    tests:
    - relationships:
        to: dim_n_portion
        field: n_portion_code
  - name: isced97_code
    description: Foreign key to dim_isced97.isced97_code, linking to isced97 dimension.
    tests:
    - relationships:
        to: dim_isced97
        field: isced97_code
  - name: quantile_code
    description: Foreign key to dim_quantile.quantile_code, linking to quantile dimension.
    tests:
    - relationships:
        to: dim_quantile
        field: quantile_code
  - name: bmi_code
    description: Foreign key to dim_bmi.bmi_code, linking to bmi dimension.
    tests:
    - relationships:
        to: dim_bmi
        field: bmi_code
  - name: lev_diff_code
    description: Foreign key to dim_lev_diff.lev_diff_code, linking to lev_diff dimension.
    tests:
    - relationships:
        to: dim_lev_diff
        field: lev_diff_code
  - name: yes_no_code
    description: Foreign key to dim_yes_no.yes_no_code, linking to yes_no dimension.
    tests:
    - relationships:
        to: dim_yes_no
        field: yes_no_code
  - name: value
    description: Measure value.
    tests:
    - not_null
  - name: status_code
    description: 'Metadata column: status_code.'
  - name: status_label
    description: 'Metadata column: status_label.'
  - name: linear_index
    description: 'Metadata column: linear_index.'
  - name: source_dataset_code
    description: Identifier of the original Eurostat dataset for this row.
    tests:
    - not_null
- name: mart_daily_consumption_of_fruit_and_vegetables
  description: 'Topic mart for ''Daily consumption of fruit and vegetables''.

    Consolidates data from 7 Eurostat fact views related to this topic: HLTH_EHIS_FV3E,
    HLTH_EHIS_FV3I, HLTH_EHIS_FV3M, HLTH_EHIS_FV3U, HLTH_EHIS_FV3B, HLTH_EHIS_FV3C,
    HLTH_EHIS_FV3D.

    Provides a unified view with harmonized dimension foreign keys and measures.

    Columns include: various conformed dimension foreign keys, value, status_code,
    status_label, linear_index, and source_dataset_code.

    Materialized as a table.'
  columns:
  - name: unit_code
    description: Foreign key to dim_unit.unit_code, linking to unit dimension.
    tests:
    - relationships:
        to: dim_unit
        field: unit_code
  - name: age_code
    description: Foreign key to dim_age.age_code, linking to age dimension.
    tests:
    - relationships:
        to: dim_age
        field: age_code
  - name: icd10_code
    description: Foreign key to dim_icd10.icd10_code, linking to icd10 dimension.
    tests:
    - relationships:
        to: dim_icd10
        field: icd10_code
  - name: time_code
    description: Foreign key to dim_time.time_code, linking to time dimension.
    tests:
    - relationships:
        to: dim_time
        field: time_code
  - name: geo_code
    description: Foreign key to dim_geo.geo_code, linking to geo dimension.
    tests:
    - relationships:
        to: dim_geo
        field: geo_code
  - name: freq_code
    description: Foreign key to dim_freq.freq_code, linking to freq dimension.
    tests:
    - relationships:
        to: dim_freq
        field: freq_code
  - name: resid_code
    description: Foreign key to dim_resid.resid_code, linking to resid dimension.
    tests:
    - relationships:
        to: dim_resid
        field: resid_code
  - name: sex_code
    description: Foreign key to dim_sex.sex_code, linking to sex dimension.
    tests:
    - relationships:
        to: dim_sex
        field: sex_code
  - name: isced11_code
    description: Foreign key to dim_isced11.isced11_code, linking to isced11 dimension.
    tests:
    - relationships:
        to: dim_isced11
        field: isced11_code
  - name: lev_limit_code
    description: Foreign key to dim_lev_limit.lev_limit_code, linking to lev_limit
      dimension.
    tests:
    - relationships:
        to: dim_lev_limit
        field: lev_limit_code
  - name: hlth_pb_code
    description: Foreign key to dim_hlth_pb.hlth_pb_code, linking to hlth_pb dimension.
    tests:
    - relationships:
        to: dim_hlth_pb
        field: hlth_pb_code
  - name: deg_urb_code
    description: Foreign key to dim_deg_urb.deg_urb_code, linking to deg_urb dimension.
    tests:
    - relationships:
        to: dim_deg_urb
        field: deg_urb_code
  - name: assist_code
    description: Foreign key to dim_assist.assist_code, linking to assist dimension.
    tests:
    - relationships:
        to: dim_assist
        field: assist_code
  - name: smoking_code
    description: Foreign key to dim_smoking.smoking_code, linking to smoking dimension.
    tests:
    - relationships:
        to: dim_smoking
        field: smoking_code
  - name: quant_inc_code
    description: Foreign key to dim_quant_inc.quant_inc_code, linking to quant_inc
      dimension.
    tests:
    - relationships:
        to: dim_quant_inc
        field: quant_inc_code
  - name: n_portion_code
    description: Foreign key to dim_n_portion.n_portion_code, linking to n_portion
      dimension.
    tests:
    - relationships:
        to: dim_n_portion
        field: n_portion_code
  - name: isced97_code
    description: Foreign key to dim_isced97.isced97_code, linking to isced97 dimension.
    tests:
    - relationships:
        to: dim_isced97
        field: isced97_code
  - name: quantile_code
    description: Foreign key to dim_quantile.quantile_code, linking to quantile dimension.
    tests:
    - relationships:
        to: dim_quantile
        field: quantile_code
  - name: bmi_code
    description: Foreign key to dim_bmi.bmi_code, linking to bmi dimension.
    tests:
    - relationships:
        to: dim_bmi
        field: bmi_code
  - name: lev_diff_code
    description: Foreign key to dim_lev_diff.lev_diff_code, linking to lev_diff dimension.
    tests:
    - relationships:
        to: dim_lev_diff
        field: lev_diff_code
  - name: yes_no_code
    description: Foreign key to dim_yes_no.yes_no_code, linking to yes_no dimension.
    tests:
    - relationships:
        to: dim_yes_no
        field: yes_no_code
  - name: value
    description: Measure value.
    tests:
    - not_null
  - name: status_code
    description: 'Metadata column: status_code.'
  - name: status_label
    description: 'Metadata column: status_label.'
  - name: linear_index
    description: 'Metadata column: linear_index.'
  - name: source_dataset_code
    description: Identifier of the original Eurostat dataset for this row.
    tests:
    - not_null
- name: mart_daily_smokers_of_cigarettes
  description: 'Topic mart for ''Daily smokers of cigarettes''.

    Consolidates data from 5 Eurostat fact views related to this topic: HLTH_EHIS_SK3E,
    HLTH_EHIS_SK3I, HLTH_EHIS_SK3U, HLTH_EHIS_DE3, HLTH_EHIS_DE4.

    Provides a unified view with harmonized dimension foreign keys and measures.

    Columns include: various conformed dimension foreign keys, value, status_code,
    status_label, linear_index, and source_dataset_code.

    Materialized as a table.'
  columns:
  - name: unit_code
    description: Foreign key to dim_unit.unit_code, linking to unit dimension.
    tests:
    - relationships:
        to: dim_unit
        field: unit_code
  - name: age_code
    description: Foreign key to dim_age.age_code, linking to age dimension.
    tests:
    - relationships:
        to: dim_age
        field: age_code
  - name: icd10_code
    description: Foreign key to dim_icd10.icd10_code, linking to icd10 dimension.
    tests:
    - relationships:
        to: dim_icd10
        field: icd10_code
  - name: time_code
    description: Foreign key to dim_time.time_code, linking to time dimension.
    tests:
    - relationships:
        to: dim_time
        field: time_code
  - name: geo_code
    description: Foreign key to dim_geo.geo_code, linking to geo dimension.
    tests:
    - relationships:
        to: dim_geo
        field: geo_code
  - name: freq_code
    description: Foreign key to dim_freq.freq_code, linking to freq dimension.
    tests:
    - relationships:
        to: dim_freq
        field: freq_code
  - name: resid_code
    description: Foreign key to dim_resid.resid_code, linking to resid dimension.
    tests:
    - relationships:
        to: dim_resid
        field: resid_code
  - name: sex_code
    description: Foreign key to dim_sex.sex_code, linking to sex dimension.
    tests:
    - relationships:
        to: dim_sex
        field: sex_code
  - name: isced11_code
    description: Foreign key to dim_isced11.isced11_code, linking to isced11 dimension.
    tests:
    - relationships:
        to: dim_isced11
        field: isced11_code
  - name: lev_limit_code
    description: Foreign key to dim_lev_limit.lev_limit_code, linking to lev_limit
      dimension.
    tests:
    - relationships:
        to: dim_lev_limit
        field: lev_limit_code
  - name: hlth_pb_code
    description: Foreign key to dim_hlth_pb.hlth_pb_code, linking to hlth_pb dimension.
    tests:
    - relationships:
        to: dim_hlth_pb
        field: hlth_pb_code
  - name: deg_urb_code
    description: Foreign key to dim_deg_urb.deg_urb_code, linking to deg_urb dimension.
    tests:
    - relationships:
        to: dim_deg_urb
        field: deg_urb_code
  - name: assist_code
    description: Foreign key to dim_assist.assist_code, linking to assist dimension.
    tests:
    - relationships:
        to: dim_assist
        field: assist_code
  - name: smoking_code
    description: Foreign key to dim_smoking.smoking_code, linking to smoking dimension.
    tests:
    - relationships:
        to: dim_smoking
        field: smoking_code
  - name: quant_inc_code
    description: Foreign key to dim_quant_inc.quant_inc_code, linking to quant_inc
      dimension.
    tests:
    - relationships:
        to: dim_quant_inc
        field: quant_inc_code
  - name: n_portion_code
    description: Foreign key to dim_n_portion.n_portion_code, linking to n_portion
      dimension.
    tests:
    - relationships:
        to: dim_n_portion
        field: n_portion_code
  - name: isced97_code
    description: Foreign key to dim_isced97.isced97_code, linking to isced97 dimension.
    tests:
    - relationships:
        to: dim_isced97
        field: isced97_code
  - name: quantile_code
    description: Foreign key to dim_quantile.quantile_code, linking to quantile dimension.
    tests:
    - relationships:
        to: dim_quantile
        field: quantile_code
  - name: bmi_code
    description: Foreign key to dim_bmi.bmi_code, linking to bmi dimension.
    tests:
    - relationships:
        to: dim_bmi
        field: bmi_code
  - name: lev_diff_code
    description: Foreign key to dim_lev_diff.lev_diff_code, linking to lev_diff dimension.
    tests:
    - relationships:
        to: dim_lev_diff
        field: lev_diff_code
  - name: yes_no_code
    description: Foreign key to dim_yes_no.yes_no_code, linking to yes_no dimension.
    tests:
    - relationships:
        to: dim_yes_no
        field: yes_no_code
  - name: value
    description: Measure value.
    tests:
    - not_null
  - name: status_code
    description: 'Metadata column: status_code.'
  - name: status_label
    description: 'Metadata column: status_label.'
  - name: linear_index
    description: 'Metadata column: linear_index.'
  - name: source_dataset_code
    description: Identifier of the original Eurostat dataset for this row.
    tests:
    - not_null
- name: mart_difficulties_in_personal_care_activities
  description: 'Topic mart for ''Difficulties in personal care activities''.

    Consolidates data from 5 Eurostat fact views related to this topic: HLTH_EHIS_PC1D,
    HLTH_EHIS_PC1E, HLTH_EHIS_PC1HCS, HLTH_EHIS_PC1I, HLTH_EHIS_PC1U.

    Provides a unified view with harmonized dimension foreign keys and measures.

    Columns include: various conformed dimension foreign keys, value, status_code,
    status_label, linear_index, and source_dataset_code.

    Materialized as a table.'
  columns:
  - name: unit_code
    description: Foreign key to dim_unit.unit_code, linking to unit dimension.
    tests:
    - relationships:
        to: dim_unit
        field: unit_code
  - name: age_code
    description: Foreign key to dim_age.age_code, linking to age dimension.
    tests:
    - relationships:
        to: dim_age
        field: age_code
  - name: icd10_code
    description: Foreign key to dim_icd10.icd10_code, linking to icd10 dimension.
    tests:
    - relationships:
        to: dim_icd10
        field: icd10_code
  - name: time_code
    description: Foreign key to dim_time.time_code, linking to time dimension.
    tests:
    - relationships:
        to: dim_time
        field: time_code
  - name: geo_code
    description: Foreign key to dim_geo.geo_code, linking to geo dimension.
    tests:
    - relationships:
        to: dim_geo
        field: geo_code
  - name: freq_code
    description: Foreign key to dim_freq.freq_code, linking to freq dimension.
    tests:
    - relationships:
        to: dim_freq
        field: freq_code
  - name: resid_code
    description: Foreign key to dim_resid.resid_code, linking to resid dimension.
    tests:
    - relationships:
        to: dim_resid
        field: resid_code
  - name: sex_code
    description: Foreign key to dim_sex.sex_code, linking to sex dimension.
    tests:
    - relationships:
        to: dim_sex
        field: sex_code
  - name: isced11_code
    description: Foreign key to dim_isced11.isced11_code, linking to isced11 dimension.
    tests:
    - relationships:
        to: dim_isced11
        field: isced11_code
  - name: lev_limit_code
    description: Foreign key to dim_lev_limit.lev_limit_code, linking to lev_limit
      dimension.
    tests:
    - relationships:
        to: dim_lev_limit
        field: lev_limit_code
  - name: hlth_pb_code
    description: Foreign key to dim_hlth_pb.hlth_pb_code, linking to hlth_pb dimension.
    tests:
    - relationships:
        to: dim_hlth_pb
        field: hlth_pb_code
  - name: deg_urb_code
    description: Foreign key to dim_deg_urb.deg_urb_code, linking to deg_urb dimension.
    tests:
    - relationships:
        to: dim_deg_urb
        field: deg_urb_code
  - name: assist_code
    description: Foreign key to dim_assist.assist_code, linking to assist dimension.
    tests:
    - relationships:
        to: dim_assist
        field: assist_code
  - name: smoking_code
    description: Foreign key to dim_smoking.smoking_code, linking to smoking dimension.
    tests:
    - relationships:
        to: dim_smoking
        field: smoking_code
  - name: quant_inc_code
    description: Foreign key to dim_quant_inc.quant_inc_code, linking to quant_inc
      dimension.
    tests:
    - relationships:
        to: dim_quant_inc
        field: quant_inc_code
  - name: n_portion_code
    description: Foreign key to dim_n_portion.n_portion_code, linking to n_portion
      dimension.
    tests:
    - relationships:
        to: dim_n_portion
        field: n_portion_code
  - name: isced97_code
    description: Foreign key to dim_isced97.isced97_code, linking to isced97 dimension.
    tests:
    - relationships:
        to: dim_isced97
        field: isced97_code
  - name: quantile_code
    description: Foreign key to dim_quantile.quantile_code, linking to quantile dimension.
    tests:
    - relationships:
        to: dim_quantile
        field: quantile_code
  - name: bmi_code
    description: Foreign key to dim_bmi.bmi_code, linking to bmi dimension.
    tests:
    - relationships:
        to: dim_bmi
        field: bmi_code
  - name: lev_diff_code
    description: Foreign key to dim_lev_diff.lev_diff_code, linking to lev_diff dimension.
    tests:
    - relationships:
        to: dim_lev_diff
        field: lev_diff_code
  - name: yes_no_code
    description: Foreign key to dim_yes_no.yes_no_code, linking to yes_no dimension.
    tests:
    - relationships:
        to: dim_yes_no
        field: yes_no_code
  - name: value
    description: Measure value.
    tests:
    - not_null
  - name: status_code
    description: 'Metadata column: status_code.'
  - name: status_label
    description: 'Metadata column: status_label.'
  - name: linear_index
    description: 'Metadata column: linear_index.'
  - name: source_dataset_code
    description: Identifier of the original Eurostat dataset for this row.
    tests:
    - not_null
- name: mart_former_daily_tobacco_smokers
  description: 'Topic mart for ''Former daily tobacco smokers''.

    Consolidates data from 2 Eurostat fact views related to this topic: HLTH_EHIS_SK2E,
    HLTH_EHIS_SK2I.

    Provides a unified view with harmonized dimension foreign keys and measures.

    Columns include: various conformed dimension foreign keys, value, status_code,
    status_label, linear_index, and source_dataset_code.

    Materialized as a table.'
  columns:
  - name: unit_code
    description: Foreign key to dim_unit.unit_code, linking to unit dimension.
    tests:
    - relationships:
        to: dim_unit
        field: unit_code
  - name: age_code
    description: Foreign key to dim_age.age_code, linking to age dimension.
    tests:
    - relationships:
        to: dim_age
        field: age_code
  - name: icd10_code
    description: Foreign key to dim_icd10.icd10_code, linking to icd10 dimension.
    tests:
    - relationships:
        to: dim_icd10
        field: icd10_code
  - name: time_code
    description: Foreign key to dim_time.time_code, linking to time dimension.
    tests:
    - relationships:
        to: dim_time
        field: time_code
  - name: geo_code
    description: Foreign key to dim_geo.geo_code, linking to geo dimension.
    tests:
    - relationships:
        to: dim_geo
        field: geo_code
  - name: freq_code
    description: Foreign key to dim_freq.freq_code, linking to freq dimension.
    tests:
    - relationships:
        to: dim_freq
        field: freq_code
  - name: resid_code
    description: Foreign key to dim_resid.resid_code, linking to resid dimension.
    tests:
    - relationships:
        to: dim_resid
        field: resid_code
  - name: sex_code
    description: Foreign key to dim_sex.sex_code, linking to sex dimension.
    tests:
    - relationships:
        to: dim_sex
        field: sex_code
  - name: isced11_code
    description: Foreign key to dim_isced11.isced11_code, linking to isced11 dimension.
    tests:
    - relationships:
        to: dim_isced11
        field: isced11_code
  - name: lev_limit_code
    description: Foreign key to dim_lev_limit.lev_limit_code, linking to lev_limit
      dimension.
    tests:
    - relationships:
        to: dim_lev_limit
        field: lev_limit_code
  - name: hlth_pb_code
    description: Foreign key to dim_hlth_pb.hlth_pb_code, linking to hlth_pb dimension.
    tests:
    - relationships:
        to: dim_hlth_pb
        field: hlth_pb_code
  - name: deg_urb_code
    description: Foreign key to dim_deg_urb.deg_urb_code, linking to deg_urb dimension.
    tests:
    - relationships:
        to: dim_deg_urb
        field: deg_urb_code
  - name: assist_code
    description: Foreign key to dim_assist.assist_code, linking to assist dimension.
    tests:
    - relationships:
        to: dim_assist
        field: assist_code
  - name: smoking_code
    description: Foreign key to dim_smoking.smoking_code, linking to smoking dimension.
    tests:
    - relationships:
        to: dim_smoking
        field: smoking_code
  - name: quant_inc_code
    description: Foreign key to dim_quant_inc.quant_inc_code, linking to quant_inc
      dimension.
    tests:
    - relationships:
        to: dim_quant_inc
        field: quant_inc_code
  - name: n_portion_code
    description: Foreign key to dim_n_portion.n_portion_code, linking to n_portion
      dimension.
    tests:
    - relationships:
        to: dim_n_portion
        field: n_portion_code
  - name: isced97_code
    description: Foreign key to dim_isced97.isced97_code, linking to isced97 dimension.
    tests:
    - relationships:
        to: dim_isced97
        field: isced97_code
  - name: quantile_code
    description: Foreign key to dim_quantile.quantile_code, linking to quantile dimension.
    tests:
    - relationships:
        to: dim_quantile
        field: quantile_code
  - name: bmi_code
    description: Foreign key to dim_bmi.bmi_code, linking to bmi dimension.
    tests:
    - relationships:
        to: dim_bmi
        field: bmi_code
  - name: lev_diff_code
    description: Foreign key to dim_lev_diff.lev_diff_code, linking to lev_diff dimension.
    tests:
    - relationships:
        to: dim_lev_diff
        field: lev_diff_code
  - name: yes_no_code
    description: Foreign key to dim_yes_no.yes_no_code, linking to yes_no dimension.
    tests:
    - relationships:
        to: dim_yes_no
        field: yes_no_code
  - name: value
    description: Measure value.
    tests:
    - not_null
  - name: status_code
    description: 'Metadata column: status_code.'
  - name: status_label
    description: 'Metadata column: status_label.'
  - name: linear_index
    description: 'Metadata column: linear_index.'
  - name: source_dataset_code
    description: Identifier of the original Eurostat dataset for this row.
    tests:
    - not_null
- name: mart_need_for_help_with_household_activities
  description: 'Topic mart for ''Need for help with household activities''.

    Consolidates data from 3 Eurostat fact views related to this topic: HLTH_EHIS_HAHLPD,
    HLTH_EHIS_HAHLPE, HLTH_EHIS_HAHLPU.

    Provides a unified view with harmonized dimension foreign keys and measures.

    Columns include: various conformed dimension foreign keys, value, status_code,
    status_label, linear_index, and source_dataset_code.

    Materialized as a table.'
  columns:
  - name: unit_code
    description: Foreign key to dim_unit.unit_code, linking to unit dimension.
    tests:
    - relationships:
        to: dim_unit
        field: unit_code
  - name: age_code
    description: Foreign key to dim_age.age_code, linking to age dimension.
    tests:
    - relationships:
        to: dim_age
        field: age_code
  - name: icd10_code
    description: Foreign key to dim_icd10.icd10_code, linking to icd10 dimension.
    tests:
    - relationships:
        to: dim_icd10
        field: icd10_code
  - name: time_code
    description: Foreign key to dim_time.time_code, linking to time dimension.
    tests:
    - relationships:
        to: dim_time
        field: time_code
  - name: geo_code
    description: Foreign key to dim_geo.geo_code, linking to geo dimension.
    tests:
    - relationships:
        to: dim_geo
        field: geo_code
  - name: freq_code
    description: Foreign key to dim_freq.freq_code, linking to freq dimension.
    tests:
    - relationships:
        to: dim_freq
        field: freq_code
  - name: resid_code
    description: Foreign key to dim_resid.resid_code, linking to resid dimension.
    tests:
    - relationships:
        to: dim_resid
        field: resid_code
  - name: sex_code
    description: Foreign key to dim_sex.sex_code, linking to sex dimension.
    tests:
    - relationships:
        to: dim_sex
        field: sex_code
  - name: isced11_code
    description: Foreign key to dim_isced11.isced11_code, linking to isced11 dimension.
    tests:
    - relationships:
        to: dim_isced11
        field: isced11_code
  - name: lev_limit_code
    description: Foreign key to dim_lev_limit.lev_limit_code, linking to lev_limit
      dimension.
    tests:
    - relationships:
        to: dim_lev_limit
        field: lev_limit_code
  - name: hlth_pb_code
    description: Foreign key to dim_hlth_pb.hlth_pb_code, linking to hlth_pb dimension.
    tests:
    - relationships:
        to: dim_hlth_pb
        field: hlth_pb_code
  - name: deg_urb_code
    description: Foreign key to dim_deg_urb.deg_urb_code, linking to deg_urb dimension.
    tests:
    - relationships:
        to: dim_deg_urb
        field: deg_urb_code
  - name: assist_code
    description: Foreign key to dim_assist.assist_code, linking to assist dimension.
    tests:
    - relationships:
        to: dim_assist
        field: assist_code
  - name: smoking_code
    description: Foreign key to dim_smoking.smoking_code, linking to smoking dimension.
    tests:
    - relationships:
        to: dim_smoking
        field: smoking_code
  - name: quant_inc_code
    description: Foreign key to dim_quant_inc.quant_inc_code, linking to quant_inc
      dimension.
    tests:
    - relationships:
        to: dim_quant_inc
        field: quant_inc_code
  - name: n_portion_code
    description: Foreign key to dim_n_portion.n_portion_code, linking to n_portion
      dimension.
    tests:
    - relationships:
        to: dim_n_portion
        field: n_portion_code
  - name: isced97_code
    description: Foreign key to dim_isced97.isced97_code, linking to isced97 dimension.
    tests:
    - relationships:
        to: dim_isced97
        field: isced97_code
  - name: quantile_code
    description: Foreign key to dim_quantile.quantile_code, linking to quantile dimension.
    tests:
    - relationships:
        to: dim_quantile
        field: quantile_code
  - name: bmi_code
    description: Foreign key to dim_bmi.bmi_code, linking to bmi dimension.
    tests:
    - relationships:
        to: dim_bmi
        field: bmi_code
  - name: lev_diff_code
    description: Foreign key to dim_lev_diff.lev_diff_code, linking to lev_diff dimension.
    tests:
    - relationships:
        to: dim_lev_diff
        field: lev_diff_code
  - name: yes_no_code
    description: Foreign key to dim_yes_no.yes_no_code, linking to yes_no dimension.
    tests:
    - relationships:
        to: dim_yes_no
        field: yes_no_code
  - name: value
    description: Measure value.
    tests:
    - not_null
  - name: status_code
    description: 'Metadata column: status_code.'
  - name: status_label
    description: 'Metadata column: status_label.'
  - name: linear_index
    description: 'Metadata column: linear_index.'
  - name: source_dataset_code
    description: Identifier of the original Eurostat dataset for this row.
    tests:
    - not_null
