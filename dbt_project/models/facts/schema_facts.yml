version: 2
models:
- name: fct_hlth_rs_phys
  description: Fact view for source table 'hlth_rs_phys'. Contains measures and foreign
    keys to conformed dimensions.
  columns:
  - name: freq_code
    description: Foreign key to dim_freq.freq_code, linking to the freq dimension.
    tests:
    - relationships:
        to: dim_freq
        field: freq_code
  - name: time_code
    description: Foreign key to dim_time.time_code, linking to the time dimension.
    tests:
    - relationships:
        to: dim_time
        field: time_code
  - name: geo_code
    description: Foreign key to dim_geo.geo_code, linking to the geo dimension.
    tests:
    - relationships:
        to: dim_geo
        field: geo_code
  - name: age_code
    description: Foreign key to dim_age.age_code, linking to the age dimension.
    tests:
    - relationships:
        to: dim_age
        field: age_code
  - name: sex_code
    description: Foreign key to dim_sex.sex_code, linking to the sex dimension.
    tests:
    - relationships:
        to: dim_sex
        field: sex_code
  - name: unit_code
    description: Foreign key to dim_unit.unit_code, linking to the unit dimension.
    tests:
    - relationships:
        to: dim_unit
        field: unit_code
  - name: med_spec_code
    description: Foreign key to dim_med_spec.med_spec_code, linking to the med_spec
      dimension.
    tests:
    - relationships:
        to: dim_med_spec
        field: med_spec_code
  - name: value
    description: Measure value from source table.
    tests:
    - not_null
  - name: source_dataset_id
    description: Metadata column source_dataset_id from source table.
- name: fct_hlth_rs_physcat
  description: Fact view for source table 'hlth_rs_physcat'. Contains measures and
    foreign keys to conformed dimensions.
  columns:
  - name: freq_code
    description: Foreign key to dim_freq.freq_code, linking to the freq dimension.
    tests:
    - relationships:
        to: dim_freq
        field: freq_code
  - name: time_code
    description: Foreign key to dim_time.time_code, linking to the time dimension.
    tests:
    - relationships:
        to: dim_time
        field: time_code
  - name: geo_code
    description: Foreign key to dim_geo.geo_code, linking to the geo dimension.
    tests:
    - relationships:
        to: dim_geo
        field: geo_code
  - name: age_code
    description: Foreign key to dim_age.age_code, linking to the age dimension.
    tests:
    - relationships:
        to: dim_age
        field: age_code
  - name: sex_code
    description: Foreign key to dim_sex.sex_code, linking to the sex dimension.
    tests:
    - relationships:
        to: dim_sex
        field: sex_code
  - name: unit_code
    description: Foreign key to dim_unit.unit_code, linking to the unit dimension.
    tests:
    - relationships:
        to: dim_unit
        field: unit_code
  - name: med_spec_code
    description: Foreign key to dim_med_spec.med_spec_code, linking to the med_spec
      dimension.
    tests:
    - relationships:
        to: dim_med_spec
        field: med_spec_code
  - name: value
    description: Measure value from source table.
    tests:
    - not_null
  - name: source_dataset_id
    description: Metadata column source_dataset_id from source table.
- name: fct_hlth_rs_physreg
  description: Fact view for source table 'hlth_rs_physreg'. Contains measures and
    foreign keys to conformed dimensions.
  columns:
  - name: freq_code
    description: Foreign key to dim_freq.freq_code, linking to the freq dimension.
    tests:
    - relationships:
        to: dim_freq
        field: freq_code
  - name: time_code
    description: Foreign key to dim_time.time_code, linking to the time dimension.
    tests:
    - relationships:
        to: dim_time
        field: time_code
  - name: geo_code
    description: Foreign key to dim_geo.geo_code, linking to the geo dimension.
    tests:
    - relationships:
        to: dim_geo
        field: geo_code
  - name: age_code
    description: Foreign key to dim_age.age_code, linking to the age dimension.
    tests:
    - relationships:
        to: dim_age
        field: age_code
  - name: sex_code
    description: Foreign key to dim_sex.sex_code, linking to the sex dimension.
    tests:
    - relationships:
        to: dim_sex
        field: sex_code
  - name: unit_code
    description: Foreign key to dim_unit.unit_code, linking to the unit dimension.
    tests:
    - relationships:
        to: dim_unit
        field: unit_code
  - name: med_spec_code
    description: Foreign key to dim_med_spec.med_spec_code, linking to the med_spec
      dimension.
    tests:
    - relationships:
        to: dim_med_spec
        field: med_spec_code
  - name: value
    description: Measure value from source table.
    tests:
    - not_null
  - name: source_dataset_id
    description: Metadata column source_dataset_id from source table.
- name: fct_hlth_rs_spec
  description: Fact view for source table 'hlth_rs_spec'. Contains measures and foreign
    keys to conformed dimensions.
  columns:
  - name: freq_code
    description: Foreign key to dim_freq.freq_code, linking to the freq dimension.
    tests:
    - relationships:
        to: dim_freq
        field: freq_code
  - name: time_code
    description: Foreign key to dim_time.time_code, linking to the time dimension.
    tests:
    - relationships:
        to: dim_time
        field: time_code
  - name: geo_code
    description: Foreign key to dim_geo.geo_code, linking to the geo dimension.
    tests:
    - relationships:
        to: dim_geo
        field: geo_code
  - name: age_code
    description: Foreign key to dim_age.age_code, linking to the age dimension.
    tests:
    - relationships:
        to: dim_age
        field: age_code
  - name: sex_code
    description: Foreign key to dim_sex.sex_code, linking to the sex dimension.
    tests:
    - relationships:
        to: dim_sex
        field: sex_code
  - name: unit_code
    description: Foreign key to dim_unit.unit_code, linking to the unit dimension.
    tests:
    - relationships:
        to: dim_unit
        field: unit_code
  - name: med_spec_code
    description: Foreign key to dim_med_spec.med_spec_code, linking to the med_spec
      dimension.
    tests:
    - relationships:
        to: dim_med_spec
        field: med_spec_code
  - name: value
    description: Measure value from source table.
    tests:
    - not_null
  - name: source_dataset_id
    description: Metadata column source_dataset_id from source table.
