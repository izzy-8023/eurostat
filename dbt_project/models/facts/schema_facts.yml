version: 2
models:
- name: fct_hlth_cd_ainfo
  description: Fact view for source table 'hlth_cd_ainfo'. Contains measures and foreign
    keys to conformed dimensions.
  columns:
  - name: geo_code
    description: Foreign key to dim_geo.geo_code, linking to the geo dimension.
    tests:
    - relationships:
        to: dim_geo
        field: geo_code
  - name: sex_code
    description: Foreign key to dim_sex.sex_code, linking to the sex dimension.
    tests:
    - relationships:
        to: dim_sex
        field: sex_code
  - name: age_code
    description: Foreign key to dim_age.age_code, linking to the age dimension.
    tests:
    - relationships:
        to: dim_age
        field: age_code
  - name: time_code
    description: Foreign key to dim_time.time_code, linking to the time dimension.
    tests:
    - relationships:
        to: dim_time
        field: time_code
  - name: freq_code
    description: Foreign key to dim_freq.freq_code, linking to the freq dimension.
    tests:
    - relationships:
        to: dim_freq
        field: freq_code
  - name: resid_code
    description: Foreign key to dim_resid.resid_code, linking to the resid dimension.
    tests:
    - relationships:
        to: dim_resid
        field: resid_code
  - name: unit_code
    description: Foreign key to dim_unit.unit_code, linking to the unit dimension.
    tests:
    - relationships:
        to: dim_unit
        field: unit_code
  - name: icd10_code
    description: Foreign key to dim_icd10.icd10_code, linking to the icd10 dimension.
    tests:
    - relationships:
        to: dim_icd10
        field: icd10_code
  - name: isced11_code
    description: Foreign key to dim_isced11.isced11_code, linking to the isced11 dimension.
    tests:
    - relationships:
        to: dim_isced11
        field: isced11_code
  - name: hlth_pb_code
    description: Foreign key to dim_hlth_pb.hlth_pb_code, linking to the hlth_pb dimension.
    tests:
    - relationships:
        to: dim_hlth_pb
        field: hlth_pb_code
  - name: lev_limit_code
    description: Foreign key to dim_lev_limit.lev_limit_code, linking to the lev_limit
      dimension.
    tests:
    - relationships:
        to: dim_lev_limit
        field: lev_limit_code
  - name: deg_urb_code
    description: Foreign key to dim_deg_urb.deg_urb_code, linking to the deg_urb dimension.
    tests:
    - relationships:
        to: dim_deg_urb
        field: deg_urb_code
  - name: assist_code
    description: Foreign key to dim_assist.assist_code, linking to the assist dimension.
    tests:
    - relationships:
        to: dim_assist
        field: assist_code
  - name: smoking_code
    description: Foreign key to dim_smoking.smoking_code, linking to the smoking dimension.
    tests:
    - relationships:
        to: dim_smoking
        field: smoking_code
  - name: quant_inc_code
    description: Foreign key to dim_quant_inc.quant_inc_code, linking to the quant_inc
      dimension.
    tests:
    - relationships:
        to: dim_quant_inc
        field: quant_inc_code
  - name: n_portion_code
    description: Foreign key to dim_n_portion.n_portion_code, linking to the n_portion
      dimension.
    tests:
    - relationships:
        to: dim_n_portion
        field: n_portion_code
  - name: isced97_code
    description: Foreign key to dim_isced97.isced97_code, linking to the isced97 dimension.
    tests:
    - relationships:
        to: dim_isced97
        field: isced97_code
  - name: quantile_code
    description: Foreign key to dim_quantile.quantile_code, linking to the quantile
      dimension.
    tests:
    - relationships:
        to: dim_quantile
        field: quantile_code
  - name: bmi_code
    description: Foreign key to dim_bmi.bmi_code, linking to the bmi dimension.
    tests:
    - relationships:
        to: dim_bmi
        field: bmi_code
  - name: lev_diff_code
    description: Foreign key to dim_lev_diff.lev_diff_code, linking to the lev_diff
      dimension.
    tests:
    - relationships:
        to: dim_lev_diff
        field: lev_diff_code
  - name: yes_no_code
    description: Foreign key to dim_yes_no.yes_no_code, linking to the yes_no dimension.
    tests:
    - relationships:
        to: dim_yes_no
        field: yes_no_code
  - name: value
    description: Measure value from source table.
    tests:
    - not_null
  - name: status_code
    description: Metadata column status_code from source table.
  - name: status_label
    description: Metadata column status_label from source table.
  - name: source_dataset_id
    description: Metadata column source_dataset_id from source table.
  - name: linear_index
    description: Metadata column linear_index from source table.
- name: fct_hlth_ehis_sk2e
  description: Fact view for source table 'hlth_ehis_sk2e'. Contains measures and
    foreign keys to conformed dimensions.
  columns:
  - name: geo_code
    description: Foreign key to dim_geo.geo_code, linking to the geo dimension.
    tests:
    - relationships:
        to: dim_geo
        field: geo_code
  - name: sex_code
    description: Foreign key to dim_sex.sex_code, linking to the sex dimension.
    tests:
    - relationships:
        to: dim_sex
        field: sex_code
  - name: age_code
    description: Foreign key to dim_age.age_code, linking to the age dimension.
    tests:
    - relationships:
        to: dim_age
        field: age_code
  - name: time_code
    description: Foreign key to dim_time.time_code, linking to the time dimension.
    tests:
    - relationships:
        to: dim_time
        field: time_code
  - name: freq_code
    description: Foreign key to dim_freq.freq_code, linking to the freq dimension.
    tests:
    - relationships:
        to: dim_freq
        field: freq_code
  - name: resid_code
    description: Foreign key to dim_resid.resid_code, linking to the resid dimension.
    tests:
    - relationships:
        to: dim_resid
        field: resid_code
  - name: unit_code
    description: Foreign key to dim_unit.unit_code, linking to the unit dimension.
    tests:
    - relationships:
        to: dim_unit
        field: unit_code
  - name: icd10_code
    description: Foreign key to dim_icd10.icd10_code, linking to the icd10 dimension.
    tests:
    - relationships:
        to: dim_icd10
        field: icd10_code
  - name: isced11_code
    description: Foreign key to dim_isced11.isced11_code, linking to the isced11 dimension.
    tests:
    - relationships:
        to: dim_isced11
        field: isced11_code
  - name: hlth_pb_code
    description: Foreign key to dim_hlth_pb.hlth_pb_code, linking to the hlth_pb dimension.
    tests:
    - relationships:
        to: dim_hlth_pb
        field: hlth_pb_code
  - name: lev_limit_code
    description: Foreign key to dim_lev_limit.lev_limit_code, linking to the lev_limit
      dimension.
    tests:
    - relationships:
        to: dim_lev_limit
        field: lev_limit_code
  - name: deg_urb_code
    description: Foreign key to dim_deg_urb.deg_urb_code, linking to the deg_urb dimension.
    tests:
    - relationships:
        to: dim_deg_urb
        field: deg_urb_code
  - name: assist_code
    description: Foreign key to dim_assist.assist_code, linking to the assist dimension.
    tests:
    - relationships:
        to: dim_assist
        field: assist_code
  - name: smoking_code
    description: Foreign key to dim_smoking.smoking_code, linking to the smoking dimension.
    tests:
    - relationships:
        to: dim_smoking
        field: smoking_code
  - name: quant_inc_code
    description: Foreign key to dim_quant_inc.quant_inc_code, linking to the quant_inc
      dimension.
    tests:
    - relationships:
        to: dim_quant_inc
        field: quant_inc_code
  - name: n_portion_code
    description: Foreign key to dim_n_portion.n_portion_code, linking to the n_portion
      dimension.
    tests:
    - relationships:
        to: dim_n_portion
        field: n_portion_code
  - name: isced97_code
    description: Foreign key to dim_isced97.isced97_code, linking to the isced97 dimension.
    tests:
    - relationships:
        to: dim_isced97
        field: isced97_code
  - name: quantile_code
    description: Foreign key to dim_quantile.quantile_code, linking to the quantile
      dimension.
    tests:
    - relationships:
        to: dim_quantile
        field: quantile_code
  - name: bmi_code
    description: Foreign key to dim_bmi.bmi_code, linking to the bmi dimension.
    tests:
    - relationships:
        to: dim_bmi
        field: bmi_code
  - name: lev_diff_code
    description: Foreign key to dim_lev_diff.lev_diff_code, linking to the lev_diff
      dimension.
    tests:
    - relationships:
        to: dim_lev_diff
        field: lev_diff_code
  - name: yes_no_code
    description: Foreign key to dim_yes_no.yes_no_code, linking to the yes_no dimension.
    tests:
    - relationships:
        to: dim_yes_no
        field: yes_no_code
  - name: value
    description: Measure value from source table.
    tests:
    - not_null
  - name: status_code
    description: Metadata column status_code from source table.
  - name: status_label
    description: Metadata column status_label from source table.
  - name: source_dataset_id
    description: Metadata column source_dataset_id from source table.
  - name: linear_index
    description: Metadata column linear_index from source table.
- name: fct_hlth_ehis_pc1e
  description: Fact view for source table 'hlth_ehis_pc1e'. Contains measures and
    foreign keys to conformed dimensions.
  columns:
  - name: geo_code
    description: Foreign key to dim_geo.geo_code, linking to the geo dimension.
    tests:
    - relationships:
        to: dim_geo
        field: geo_code
  - name: sex_code
    description: Foreign key to dim_sex.sex_code, linking to the sex dimension.
    tests:
    - relationships:
        to: dim_sex
        field: sex_code
  - name: age_code
    description: Foreign key to dim_age.age_code, linking to the age dimension.
    tests:
    - relationships:
        to: dim_age
        field: age_code
  - name: time_code
    description: Foreign key to dim_time.time_code, linking to the time dimension.
    tests:
    - relationships:
        to: dim_time
        field: time_code
  - name: freq_code
    description: Foreign key to dim_freq.freq_code, linking to the freq dimension.
    tests:
    - relationships:
        to: dim_freq
        field: freq_code
  - name: resid_code
    description: Foreign key to dim_resid.resid_code, linking to the resid dimension.
    tests:
    - relationships:
        to: dim_resid
        field: resid_code
  - name: unit_code
    description: Foreign key to dim_unit.unit_code, linking to the unit dimension.
    tests:
    - relationships:
        to: dim_unit
        field: unit_code
  - name: icd10_code
    description: Foreign key to dim_icd10.icd10_code, linking to the icd10 dimension.
    tests:
    - relationships:
        to: dim_icd10
        field: icd10_code
  - name: isced11_code
    description: Foreign key to dim_isced11.isced11_code, linking to the isced11 dimension.
    tests:
    - relationships:
        to: dim_isced11
        field: isced11_code
  - name: hlth_pb_code
    description: Foreign key to dim_hlth_pb.hlth_pb_code, linking to the hlth_pb dimension.
    tests:
    - relationships:
        to: dim_hlth_pb
        field: hlth_pb_code
  - name: lev_limit_code
    description: Foreign key to dim_lev_limit.lev_limit_code, linking to the lev_limit
      dimension.
    tests:
    - relationships:
        to: dim_lev_limit
        field: lev_limit_code
  - name: deg_urb_code
    description: Foreign key to dim_deg_urb.deg_urb_code, linking to the deg_urb dimension.
    tests:
    - relationships:
        to: dim_deg_urb
        field: deg_urb_code
  - name: assist_code
    description: Foreign key to dim_assist.assist_code, linking to the assist dimension.
    tests:
    - relationships:
        to: dim_assist
        field: assist_code
  - name: smoking_code
    description: Foreign key to dim_smoking.smoking_code, linking to the smoking dimension.
    tests:
    - relationships:
        to: dim_smoking
        field: smoking_code
  - name: quant_inc_code
    description: Foreign key to dim_quant_inc.quant_inc_code, linking to the quant_inc
      dimension.
    tests:
    - relationships:
        to: dim_quant_inc
        field: quant_inc_code
  - name: n_portion_code
    description: Foreign key to dim_n_portion.n_portion_code, linking to the n_portion
      dimension.
    tests:
    - relationships:
        to: dim_n_portion
        field: n_portion_code
  - name: isced97_code
    description: Foreign key to dim_isced97.isced97_code, linking to the isced97 dimension.
    tests:
    - relationships:
        to: dim_isced97
        field: isced97_code
  - name: quantile_code
    description: Foreign key to dim_quantile.quantile_code, linking to the quantile
      dimension.
    tests:
    - relationships:
        to: dim_quantile
        field: quantile_code
  - name: bmi_code
    description: Foreign key to dim_bmi.bmi_code, linking to the bmi dimension.
    tests:
    - relationships:
        to: dim_bmi
        field: bmi_code
  - name: lev_diff_code
    description: Foreign key to dim_lev_diff.lev_diff_code, linking to the lev_diff
      dimension.
    tests:
    - relationships:
        to: dim_lev_diff
        field: lev_diff_code
  - name: yes_no_code
    description: Foreign key to dim_yes_no.yes_no_code, linking to the yes_no dimension.
    tests:
    - relationships:
        to: dim_yes_no
        field: yes_no_code
  - name: value
    description: Measure value from source table.
    tests:
    - not_null
  - name: status_code
    description: Metadata column status_code from source table.
  - name: status_label
    description: Metadata column status_label from source table.
  - name: source_dataset_id
    description: Metadata column source_dataset_id from source table.
  - name: linear_index
    description: Metadata column linear_index from source table.
- name: fct_hlth_ehis_pc1u
  description: Fact view for source table 'hlth_ehis_pc1u'. Contains measures and
    foreign keys to conformed dimensions.
  columns:
  - name: geo_code
    description: Foreign key to dim_geo.geo_code, linking to the geo dimension.
    tests:
    - relationships:
        to: dim_geo
        field: geo_code
  - name: sex_code
    description: Foreign key to dim_sex.sex_code, linking to the sex dimension.
    tests:
    - relationships:
        to: dim_sex
        field: sex_code
  - name: age_code
    description: Foreign key to dim_age.age_code, linking to the age dimension.
    tests:
    - relationships:
        to: dim_age
        field: age_code
  - name: time_code
    description: Foreign key to dim_time.time_code, linking to the time dimension.
    tests:
    - relationships:
        to: dim_time
        field: time_code
  - name: freq_code
    description: Foreign key to dim_freq.freq_code, linking to the freq dimension.
    tests:
    - relationships:
        to: dim_freq
        field: freq_code
  - name: resid_code
    description: Foreign key to dim_resid.resid_code, linking to the resid dimension.
    tests:
    - relationships:
        to: dim_resid
        field: resid_code
  - name: unit_code
    description: Foreign key to dim_unit.unit_code, linking to the unit dimension.
    tests:
    - relationships:
        to: dim_unit
        field: unit_code
  - name: icd10_code
    description: Foreign key to dim_icd10.icd10_code, linking to the icd10 dimension.
    tests:
    - relationships:
        to: dim_icd10
        field: icd10_code
  - name: isced11_code
    description: Foreign key to dim_isced11.isced11_code, linking to the isced11 dimension.
    tests:
    - relationships:
        to: dim_isced11
        field: isced11_code
  - name: hlth_pb_code
    description: Foreign key to dim_hlth_pb.hlth_pb_code, linking to the hlth_pb dimension.
    tests:
    - relationships:
        to: dim_hlth_pb
        field: hlth_pb_code
  - name: lev_limit_code
    description: Foreign key to dim_lev_limit.lev_limit_code, linking to the lev_limit
      dimension.
    tests:
    - relationships:
        to: dim_lev_limit
        field: lev_limit_code
  - name: deg_urb_code
    description: Foreign key to dim_deg_urb.deg_urb_code, linking to the deg_urb dimension.
    tests:
    - relationships:
        to: dim_deg_urb
        field: deg_urb_code
  - name: assist_code
    description: Foreign key to dim_assist.assist_code, linking to the assist dimension.
    tests:
    - relationships:
        to: dim_assist
        field: assist_code
  - name: smoking_code
    description: Foreign key to dim_smoking.smoking_code, linking to the smoking dimension.
    tests:
    - relationships:
        to: dim_smoking
        field: smoking_code
  - name: quant_inc_code
    description: Foreign key to dim_quant_inc.quant_inc_code, linking to the quant_inc
      dimension.
    tests:
    - relationships:
        to: dim_quant_inc
        field: quant_inc_code
  - name: n_portion_code
    description: Foreign key to dim_n_portion.n_portion_code, linking to the n_portion
      dimension.
    tests:
    - relationships:
        to: dim_n_portion
        field: n_portion_code
  - name: isced97_code
    description: Foreign key to dim_isced97.isced97_code, linking to the isced97 dimension.
    tests:
    - relationships:
        to: dim_isced97
        field: isced97_code
  - name: quantile_code
    description: Foreign key to dim_quantile.quantile_code, linking to the quantile
      dimension.
    tests:
    - relationships:
        to: dim_quantile
        field: quantile_code
  - name: bmi_code
    description: Foreign key to dim_bmi.bmi_code, linking to the bmi dimension.
    tests:
    - relationships:
        to: dim_bmi
        field: bmi_code
  - name: lev_diff_code
    description: Foreign key to dim_lev_diff.lev_diff_code, linking to the lev_diff
      dimension.
    tests:
    - relationships:
        to: dim_lev_diff
        field: lev_diff_code
  - name: yes_no_code
    description: Foreign key to dim_yes_no.yes_no_code, linking to the yes_no dimension.
    tests:
    - relationships:
        to: dim_yes_no
        field: yes_no_code
  - name: value
    description: Measure value from source table.
    tests:
    - not_null
  - name: status_code
    description: Metadata column status_code from source table.
  - name: status_label
    description: Metadata column status_label from source table.
  - name: source_dataset_id
    description: Metadata column source_dataset_id from source table.
  - name: linear_index
    description: Metadata column linear_index from source table.
- name: fct_hlth_ehis_hahlpd
  description: Fact view for source table 'hlth_ehis_hahlpd'. Contains measures and
    foreign keys to conformed dimensions.
  columns:
  - name: geo_code
    description: Foreign key to dim_geo.geo_code, linking to the geo dimension.
    tests:
    - relationships:
        to: dim_geo
        field: geo_code
  - name: sex_code
    description: Foreign key to dim_sex.sex_code, linking to the sex dimension.
    tests:
    - relationships:
        to: dim_sex
        field: sex_code
  - name: age_code
    description: Foreign key to dim_age.age_code, linking to the age dimension.
    tests:
    - relationships:
        to: dim_age
        field: age_code
  - name: time_code
    description: Foreign key to dim_time.time_code, linking to the time dimension.
    tests:
    - relationships:
        to: dim_time
        field: time_code
  - name: freq_code
    description: Foreign key to dim_freq.freq_code, linking to the freq dimension.
    tests:
    - relationships:
        to: dim_freq
        field: freq_code
  - name: resid_code
    description: Foreign key to dim_resid.resid_code, linking to the resid dimension.
    tests:
    - relationships:
        to: dim_resid
        field: resid_code
  - name: unit_code
    description: Foreign key to dim_unit.unit_code, linking to the unit dimension.
    tests:
    - relationships:
        to: dim_unit
        field: unit_code
  - name: icd10_code
    description: Foreign key to dim_icd10.icd10_code, linking to the icd10 dimension.
    tests:
    - relationships:
        to: dim_icd10
        field: icd10_code
  - name: isced11_code
    description: Foreign key to dim_isced11.isced11_code, linking to the isced11 dimension.
    tests:
    - relationships:
        to: dim_isced11
        field: isced11_code
  - name: hlth_pb_code
    description: Foreign key to dim_hlth_pb.hlth_pb_code, linking to the hlth_pb dimension.
    tests:
    - relationships:
        to: dim_hlth_pb
        field: hlth_pb_code
  - name: lev_limit_code
    description: Foreign key to dim_lev_limit.lev_limit_code, linking to the lev_limit
      dimension.
    tests:
    - relationships:
        to: dim_lev_limit
        field: lev_limit_code
  - name: deg_urb_code
    description: Foreign key to dim_deg_urb.deg_urb_code, linking to the deg_urb dimension.
    tests:
    - relationships:
        to: dim_deg_urb
        field: deg_urb_code
  - name: assist_code
    description: Foreign key to dim_assist.assist_code, linking to the assist dimension.
    tests:
    - relationships:
        to: dim_assist
        field: assist_code
  - name: smoking_code
    description: Foreign key to dim_smoking.smoking_code, linking to the smoking dimension.
    tests:
    - relationships:
        to: dim_smoking
        field: smoking_code
  - name: quant_inc_code
    description: Foreign key to dim_quant_inc.quant_inc_code, linking to the quant_inc
      dimension.
    tests:
    - relationships:
        to: dim_quant_inc
        field: quant_inc_code
  - name: n_portion_code
    description: Foreign key to dim_n_portion.n_portion_code, linking to the n_portion
      dimension.
    tests:
    - relationships:
        to: dim_n_portion
        field: n_portion_code
  - name: isced97_code
    description: Foreign key to dim_isced97.isced97_code, linking to the isced97 dimension.
    tests:
    - relationships:
        to: dim_isced97
        field: isced97_code
  - name: quantile_code
    description: Foreign key to dim_quantile.quantile_code, linking to the quantile
      dimension.
    tests:
    - relationships:
        to: dim_quantile
        field: quantile_code
  - name: bmi_code
    description: Foreign key to dim_bmi.bmi_code, linking to the bmi dimension.
    tests:
    - relationships:
        to: dim_bmi
        field: bmi_code
  - name: lev_diff_code
    description: Foreign key to dim_lev_diff.lev_diff_code, linking to the lev_diff
      dimension.
    tests:
    - relationships:
        to: dim_lev_diff
        field: lev_diff_code
  - name: yes_no_code
    description: Foreign key to dim_yes_no.yes_no_code, linking to the yes_no dimension.
    tests:
    - relationships:
        to: dim_yes_no
        field: yes_no_code
  - name: value
    description: Measure value from source table.
    tests:
    - not_null
  - name: status_code
    description: Metadata column status_code from source table.
  - name: status_label
    description: Metadata column status_label from source table.
  - name: source_dataset_id
    description: Metadata column source_dataset_id from source table.
  - name: linear_index
    description: Metadata column linear_index from source table.
- name: fct_hlth_ehis_sk3e
  description: Fact view for source table 'hlth_ehis_sk3e'. Contains measures and
    foreign keys to conformed dimensions.
  columns:
  - name: geo_code
    description: Foreign key to dim_geo.geo_code, linking to the geo dimension.
    tests:
    - relationships:
        to: dim_geo
        field: geo_code
  - name: sex_code
    description: Foreign key to dim_sex.sex_code, linking to the sex dimension.
    tests:
    - relationships:
        to: dim_sex
        field: sex_code
  - name: age_code
    description: Foreign key to dim_age.age_code, linking to the age dimension.
    tests:
    - relationships:
        to: dim_age
        field: age_code
  - name: time_code
    description: Foreign key to dim_time.time_code, linking to the time dimension.
    tests:
    - relationships:
        to: dim_time
        field: time_code
  - name: freq_code
    description: Foreign key to dim_freq.freq_code, linking to the freq dimension.
    tests:
    - relationships:
        to: dim_freq
        field: freq_code
  - name: resid_code
    description: Foreign key to dim_resid.resid_code, linking to the resid dimension.
    tests:
    - relationships:
        to: dim_resid
        field: resid_code
  - name: unit_code
    description: Foreign key to dim_unit.unit_code, linking to the unit dimension.
    tests:
    - relationships:
        to: dim_unit
        field: unit_code
  - name: icd10_code
    description: Foreign key to dim_icd10.icd10_code, linking to the icd10 dimension.
    tests:
    - relationships:
        to: dim_icd10
        field: icd10_code
  - name: isced11_code
    description: Foreign key to dim_isced11.isced11_code, linking to the isced11 dimension.
    tests:
    - relationships:
        to: dim_isced11
        field: isced11_code
  - name: hlth_pb_code
    description: Foreign key to dim_hlth_pb.hlth_pb_code, linking to the hlth_pb dimension.
    tests:
    - relationships:
        to: dim_hlth_pb
        field: hlth_pb_code
  - name: lev_limit_code
    description: Foreign key to dim_lev_limit.lev_limit_code, linking to the lev_limit
      dimension.
    tests:
    - relationships:
        to: dim_lev_limit
        field: lev_limit_code
  - name: deg_urb_code
    description: Foreign key to dim_deg_urb.deg_urb_code, linking to the deg_urb dimension.
    tests:
    - relationships:
        to: dim_deg_urb
        field: deg_urb_code
  - name: assist_code
    description: Foreign key to dim_assist.assist_code, linking to the assist dimension.
    tests:
    - relationships:
        to: dim_assist
        field: assist_code
  - name: smoking_code
    description: Foreign key to dim_smoking.smoking_code, linking to the smoking dimension.
    tests:
    - relationships:
        to: dim_smoking
        field: smoking_code
  - name: quant_inc_code
    description: Foreign key to dim_quant_inc.quant_inc_code, linking to the quant_inc
      dimension.
    tests:
    - relationships:
        to: dim_quant_inc
        field: quant_inc_code
  - name: n_portion_code
    description: Foreign key to dim_n_portion.n_portion_code, linking to the n_portion
      dimension.
    tests:
    - relationships:
        to: dim_n_portion
        field: n_portion_code
  - name: isced97_code
    description: Foreign key to dim_isced97.isced97_code, linking to the isced97 dimension.
    tests:
    - relationships:
        to: dim_isced97
        field: isced97_code
  - name: quantile_code
    description: Foreign key to dim_quantile.quantile_code, linking to the quantile
      dimension.
    tests:
    - relationships:
        to: dim_quantile
        field: quantile_code
  - name: bmi_code
    description: Foreign key to dim_bmi.bmi_code, linking to the bmi dimension.
    tests:
    - relationships:
        to: dim_bmi
        field: bmi_code
  - name: lev_diff_code
    description: Foreign key to dim_lev_diff.lev_diff_code, linking to the lev_diff
      dimension.
    tests:
    - relationships:
        to: dim_lev_diff
        field: lev_diff_code
  - name: yes_no_code
    description: Foreign key to dim_yes_no.yes_no_code, linking to the yes_no dimension.
    tests:
    - relationships:
        to: dim_yes_no
        field: yes_no_code
  - name: value
    description: Measure value from source table.
    tests:
    - not_null
  - name: status_code
    description: Metadata column status_code from source table.
  - name: status_label
    description: Metadata column status_label from source table.
  - name: source_dataset_id
    description: Metadata column source_dataset_id from source table.
  - name: linear_index
    description: Metadata column linear_index from source table.
- name: fct_hlth_ehis_sk2i
  description: Fact view for source table 'hlth_ehis_sk2i'. Contains measures and
    foreign keys to conformed dimensions.
  columns:
  - name: geo_code
    description: Foreign key to dim_geo.geo_code, linking to the geo dimension.
    tests:
    - relationships:
        to: dim_geo
        field: geo_code
  - name: sex_code
    description: Foreign key to dim_sex.sex_code, linking to the sex dimension.
    tests:
    - relationships:
        to: dim_sex
        field: sex_code
  - name: age_code
    description: Foreign key to dim_age.age_code, linking to the age dimension.
    tests:
    - relationships:
        to: dim_age
        field: age_code
  - name: time_code
    description: Foreign key to dim_time.time_code, linking to the time dimension.
    tests:
    - relationships:
        to: dim_time
        field: time_code
  - name: freq_code
    description: Foreign key to dim_freq.freq_code, linking to the freq dimension.
    tests:
    - relationships:
        to: dim_freq
        field: freq_code
  - name: resid_code
    description: Foreign key to dim_resid.resid_code, linking to the resid dimension.
    tests:
    - relationships:
        to: dim_resid
        field: resid_code
  - name: unit_code
    description: Foreign key to dim_unit.unit_code, linking to the unit dimension.
    tests:
    - relationships:
        to: dim_unit
        field: unit_code
  - name: icd10_code
    description: Foreign key to dim_icd10.icd10_code, linking to the icd10 dimension.
    tests:
    - relationships:
        to: dim_icd10
        field: icd10_code
  - name: isced11_code
    description: Foreign key to dim_isced11.isced11_code, linking to the isced11 dimension.
    tests:
    - relationships:
        to: dim_isced11
        field: isced11_code
  - name: hlth_pb_code
    description: Foreign key to dim_hlth_pb.hlth_pb_code, linking to the hlth_pb dimension.
    tests:
    - relationships:
        to: dim_hlth_pb
        field: hlth_pb_code
  - name: lev_limit_code
    description: Foreign key to dim_lev_limit.lev_limit_code, linking to the lev_limit
      dimension.
    tests:
    - relationships:
        to: dim_lev_limit
        field: lev_limit_code
  - name: deg_urb_code
    description: Foreign key to dim_deg_urb.deg_urb_code, linking to the deg_urb dimension.
    tests:
    - relationships:
        to: dim_deg_urb
        field: deg_urb_code
  - name: assist_code
    description: Foreign key to dim_assist.assist_code, linking to the assist dimension.
    tests:
    - relationships:
        to: dim_assist
        field: assist_code
  - name: smoking_code
    description: Foreign key to dim_smoking.smoking_code, linking to the smoking dimension.
    tests:
    - relationships:
        to: dim_smoking
        field: smoking_code
  - name: quant_inc_code
    description: Foreign key to dim_quant_inc.quant_inc_code, linking to the quant_inc
      dimension.
    tests:
    - relationships:
        to: dim_quant_inc
        field: quant_inc_code
  - name: n_portion_code
    description: Foreign key to dim_n_portion.n_portion_code, linking to the n_portion
      dimension.
    tests:
    - relationships:
        to: dim_n_portion
        field: n_portion_code
  - name: isced97_code
    description: Foreign key to dim_isced97.isced97_code, linking to the isced97 dimension.
    tests:
    - relationships:
        to: dim_isced97
        field: isced97_code
  - name: quantile_code
    description: Foreign key to dim_quantile.quantile_code, linking to the quantile
      dimension.
    tests:
    - relationships:
        to: dim_quantile
        field: quantile_code
  - name: bmi_code
    description: Foreign key to dim_bmi.bmi_code, linking to the bmi dimension.
    tests:
    - relationships:
        to: dim_bmi
        field: bmi_code
  - name: lev_diff_code
    description: Foreign key to dim_lev_diff.lev_diff_code, linking to the lev_diff
      dimension.
    tests:
    - relationships:
        to: dim_lev_diff
        field: lev_diff_code
  - name: yes_no_code
    description: Foreign key to dim_yes_no.yes_no_code, linking to the yes_no dimension.
    tests:
    - relationships:
        to: dim_yes_no
        field: yes_no_code
  - name: value
    description: Measure value from source table.
    tests:
    - not_null
  - name: status_code
    description: Metadata column status_code from source table.
  - name: status_label
    description: Metadata column status_label from source table.
  - name: source_dataset_id
    description: Metadata column source_dataset_id from source table.
  - name: linear_index
    description: Metadata column linear_index from source table.
- name: fct_hlth_ehis_hahlpe
  description: Fact view for source table 'hlth_ehis_hahlpe'. Contains measures and
    foreign keys to conformed dimensions.
  columns:
  - name: geo_code
    description: Foreign key to dim_geo.geo_code, linking to the geo dimension.
    tests:
    - relationships:
        to: dim_geo
        field: geo_code
  - name: sex_code
    description: Foreign key to dim_sex.sex_code, linking to the sex dimension.
    tests:
    - relationships:
        to: dim_sex
        field: sex_code
  - name: age_code
    description: Foreign key to dim_age.age_code, linking to the age dimension.
    tests:
    - relationships:
        to: dim_age
        field: age_code
  - name: time_code
    description: Foreign key to dim_time.time_code, linking to the time dimension.
    tests:
    - relationships:
        to: dim_time
        field: time_code
  - name: freq_code
    description: Foreign key to dim_freq.freq_code, linking to the freq dimension.
    tests:
    - relationships:
        to: dim_freq
        field: freq_code
  - name: resid_code
    description: Foreign key to dim_resid.resid_code, linking to the resid dimension.
    tests:
    - relationships:
        to: dim_resid
        field: resid_code
  - name: unit_code
    description: Foreign key to dim_unit.unit_code, linking to the unit dimension.
    tests:
    - relationships:
        to: dim_unit
        field: unit_code
  - name: icd10_code
    description: Foreign key to dim_icd10.icd10_code, linking to the icd10 dimension.
    tests:
    - relationships:
        to: dim_icd10
        field: icd10_code
  - name: isced11_code
    description: Foreign key to dim_isced11.isced11_code, linking to the isced11 dimension.
    tests:
    - relationships:
        to: dim_isced11
        field: isced11_code
  - name: hlth_pb_code
    description: Foreign key to dim_hlth_pb.hlth_pb_code, linking to the hlth_pb dimension.
    tests:
    - relationships:
        to: dim_hlth_pb
        field: hlth_pb_code
  - name: lev_limit_code
    description: Foreign key to dim_lev_limit.lev_limit_code, linking to the lev_limit
      dimension.
    tests:
    - relationships:
        to: dim_lev_limit
        field: lev_limit_code
  - name: deg_urb_code
    description: Foreign key to dim_deg_urb.deg_urb_code, linking to the deg_urb dimension.
    tests:
    - relationships:
        to: dim_deg_urb
        field: deg_urb_code
  - name: assist_code
    description: Foreign key to dim_assist.assist_code, linking to the assist dimension.
    tests:
    - relationships:
        to: dim_assist
        field: assist_code
  - name: smoking_code
    description: Foreign key to dim_smoking.smoking_code, linking to the smoking dimension.
    tests:
    - relationships:
        to: dim_smoking
        field: smoking_code
  - name: quant_inc_code
    description: Foreign key to dim_quant_inc.quant_inc_code, linking to the quant_inc
      dimension.
    tests:
    - relationships:
        to: dim_quant_inc
        field: quant_inc_code
  - name: n_portion_code
    description: Foreign key to dim_n_portion.n_portion_code, linking to the n_portion
      dimension.
    tests:
    - relationships:
        to: dim_n_portion
        field: n_portion_code
  - name: isced97_code
    description: Foreign key to dim_isced97.isced97_code, linking to the isced97 dimension.
    tests:
    - relationships:
        to: dim_isced97
        field: isced97_code
  - name: quantile_code
    description: Foreign key to dim_quantile.quantile_code, linking to the quantile
      dimension.
    tests:
    - relationships:
        to: dim_quantile
        field: quantile_code
  - name: bmi_code
    description: Foreign key to dim_bmi.bmi_code, linking to the bmi dimension.
    tests:
    - relationships:
        to: dim_bmi
        field: bmi_code
  - name: lev_diff_code
    description: Foreign key to dim_lev_diff.lev_diff_code, linking to the lev_diff
      dimension.
    tests:
    - relationships:
        to: dim_lev_diff
        field: lev_diff_code
  - name: yes_no_code
    description: Foreign key to dim_yes_no.yes_no_code, linking to the yes_no dimension.
    tests:
    - relationships:
        to: dim_yes_no
        field: yes_no_code
  - name: value
    description: Measure value from source table.
    tests:
    - not_null
  - name: status_code
    description: Metadata column status_code from source table.
  - name: status_label
    description: Metadata column status_label from source table.
  - name: source_dataset_id
    description: Metadata column source_dataset_id from source table.
  - name: linear_index
    description: Metadata column linear_index from source table.
- name: fct_hlth_ehis_fv3e
  description: Fact view for source table 'hlth_ehis_fv3e'. Contains measures and
    foreign keys to conformed dimensions.
  columns:
  - name: geo_code
    description: Foreign key to dim_geo.geo_code, linking to the geo dimension.
    tests:
    - relationships:
        to: dim_geo
        field: geo_code
  - name: sex_code
    description: Foreign key to dim_sex.sex_code, linking to the sex dimension.
    tests:
    - relationships:
        to: dim_sex
        field: sex_code
  - name: age_code
    description: Foreign key to dim_age.age_code, linking to the age dimension.
    tests:
    - relationships:
        to: dim_age
        field: age_code
  - name: time_code
    description: Foreign key to dim_time.time_code, linking to the time dimension.
    tests:
    - relationships:
        to: dim_time
        field: time_code
  - name: freq_code
    description: Foreign key to dim_freq.freq_code, linking to the freq dimension.
    tests:
    - relationships:
        to: dim_freq
        field: freq_code
  - name: resid_code
    description: Foreign key to dim_resid.resid_code, linking to the resid dimension.
    tests:
    - relationships:
        to: dim_resid
        field: resid_code
  - name: unit_code
    description: Foreign key to dim_unit.unit_code, linking to the unit dimension.
    tests:
    - relationships:
        to: dim_unit
        field: unit_code
  - name: icd10_code
    description: Foreign key to dim_icd10.icd10_code, linking to the icd10 dimension.
    tests:
    - relationships:
        to: dim_icd10
        field: icd10_code
  - name: isced11_code
    description: Foreign key to dim_isced11.isced11_code, linking to the isced11 dimension.
    tests:
    - relationships:
        to: dim_isced11
        field: isced11_code
  - name: hlth_pb_code
    description: Foreign key to dim_hlth_pb.hlth_pb_code, linking to the hlth_pb dimension.
    tests:
    - relationships:
        to: dim_hlth_pb
        field: hlth_pb_code
  - name: lev_limit_code
    description: Foreign key to dim_lev_limit.lev_limit_code, linking to the lev_limit
      dimension.
    tests:
    - relationships:
        to: dim_lev_limit
        field: lev_limit_code
  - name: deg_urb_code
    description: Foreign key to dim_deg_urb.deg_urb_code, linking to the deg_urb dimension.
    tests:
    - relationships:
        to: dim_deg_urb
        field: deg_urb_code
  - name: assist_code
    description: Foreign key to dim_assist.assist_code, linking to the assist dimension.
    tests:
    - relationships:
        to: dim_assist
        field: assist_code
  - name: smoking_code
    description: Foreign key to dim_smoking.smoking_code, linking to the smoking dimension.
    tests:
    - relationships:
        to: dim_smoking
        field: smoking_code
  - name: quant_inc_code
    description: Foreign key to dim_quant_inc.quant_inc_code, linking to the quant_inc
      dimension.
    tests:
    - relationships:
        to: dim_quant_inc
        field: quant_inc_code
  - name: n_portion_code
    description: Foreign key to dim_n_portion.n_portion_code, linking to the n_portion
      dimension.
    tests:
    - relationships:
        to: dim_n_portion
        field: n_portion_code
  - name: isced97_code
    description: Foreign key to dim_isced97.isced97_code, linking to the isced97 dimension.
    tests:
    - relationships:
        to: dim_isced97
        field: isced97_code
  - name: quantile_code
    description: Foreign key to dim_quantile.quantile_code, linking to the quantile
      dimension.
    tests:
    - relationships:
        to: dim_quantile
        field: quantile_code
  - name: bmi_code
    description: Foreign key to dim_bmi.bmi_code, linking to the bmi dimension.
    tests:
    - relationships:
        to: dim_bmi
        field: bmi_code
  - name: lev_diff_code
    description: Foreign key to dim_lev_diff.lev_diff_code, linking to the lev_diff
      dimension.
    tests:
    - relationships:
        to: dim_lev_diff
        field: lev_diff_code
  - name: yes_no_code
    description: Foreign key to dim_yes_no.yes_no_code, linking to the yes_no dimension.
    tests:
    - relationships:
        to: dim_yes_no
        field: yes_no_code
  - name: value
    description: Measure value from source table.
    tests:
    - not_null
  - name: status_code
    description: Metadata column status_code from source table.
  - name: status_label
    description: Metadata column status_label from source table.
  - name: source_dataset_id
    description: Metadata column source_dataset_id from source table.
  - name: linear_index
    description: Metadata column linear_index from source table.
- name: fct_hlth_ehis_sk3i
  description: Fact view for source table 'hlth_ehis_sk3i'. Contains measures and
    foreign keys to conformed dimensions.
  columns:
  - name: geo_code
    description: Foreign key to dim_geo.geo_code, linking to the geo dimension.
    tests:
    - relationships:
        to: dim_geo
        field: geo_code
  - name: sex_code
    description: Foreign key to dim_sex.sex_code, linking to the sex dimension.
    tests:
    - relationships:
        to: dim_sex
        field: sex_code
  - name: age_code
    description: Foreign key to dim_age.age_code, linking to the age dimension.
    tests:
    - relationships:
        to: dim_age
        field: age_code
  - name: time_code
    description: Foreign key to dim_time.time_code, linking to the time dimension.
    tests:
    - relationships:
        to: dim_time
        field: time_code
  - name: freq_code
    description: Foreign key to dim_freq.freq_code, linking to the freq dimension.
    tests:
    - relationships:
        to: dim_freq
        field: freq_code
  - name: resid_code
    description: Foreign key to dim_resid.resid_code, linking to the resid dimension.
    tests:
    - relationships:
        to: dim_resid
        field: resid_code
  - name: unit_code
    description: Foreign key to dim_unit.unit_code, linking to the unit dimension.
    tests:
    - relationships:
        to: dim_unit
        field: unit_code
  - name: icd10_code
    description: Foreign key to dim_icd10.icd10_code, linking to the icd10 dimension.
    tests:
    - relationships:
        to: dim_icd10
        field: icd10_code
  - name: isced11_code
    description: Foreign key to dim_isced11.isced11_code, linking to the isced11 dimension.
    tests:
    - relationships:
        to: dim_isced11
        field: isced11_code
  - name: hlth_pb_code
    description: Foreign key to dim_hlth_pb.hlth_pb_code, linking to the hlth_pb dimension.
    tests:
    - relationships:
        to: dim_hlth_pb
        field: hlth_pb_code
  - name: lev_limit_code
    description: Foreign key to dim_lev_limit.lev_limit_code, linking to the lev_limit
      dimension.
    tests:
    - relationships:
        to: dim_lev_limit
        field: lev_limit_code
  - name: deg_urb_code
    description: Foreign key to dim_deg_urb.deg_urb_code, linking to the deg_urb dimension.
    tests:
    - relationships:
        to: dim_deg_urb
        field: deg_urb_code
  - name: assist_code
    description: Foreign key to dim_assist.assist_code, linking to the assist dimension.
    tests:
    - relationships:
        to: dim_assist
        field: assist_code
  - name: smoking_code
    description: Foreign key to dim_smoking.smoking_code, linking to the smoking dimension.
    tests:
    - relationships:
        to: dim_smoking
        field: smoking_code
  - name: quant_inc_code
    description: Foreign key to dim_quant_inc.quant_inc_code, linking to the quant_inc
      dimension.
    tests:
    - relationships:
        to: dim_quant_inc
        field: quant_inc_code
  - name: n_portion_code
    description: Foreign key to dim_n_portion.n_portion_code, linking to the n_portion
      dimension.
    tests:
    - relationships:
        to: dim_n_portion
        field: n_portion_code
  - name: isced97_code
    description: Foreign key to dim_isced97.isced97_code, linking to the isced97 dimension.
    tests:
    - relationships:
        to: dim_isced97
        field: isced97_code
  - name: quantile_code
    description: Foreign key to dim_quantile.quantile_code, linking to the quantile
      dimension.
    tests:
    - relationships:
        to: dim_quantile
        field: quantile_code
  - name: bmi_code
    description: Foreign key to dim_bmi.bmi_code, linking to the bmi dimension.
    tests:
    - relationships:
        to: dim_bmi
        field: bmi_code
  - name: lev_diff_code
    description: Foreign key to dim_lev_diff.lev_diff_code, linking to the lev_diff
      dimension.
    tests:
    - relationships:
        to: dim_lev_diff
        field: lev_diff_code
  - name: yes_no_code
    description: Foreign key to dim_yes_no.yes_no_code, linking to the yes_no dimension.
    tests:
    - relationships:
        to: dim_yes_no
        field: yes_no_code
  - name: value
    description: Measure value from source table.
    tests:
    - not_null
  - name: status_code
    description: Metadata column status_code from source table.
  - name: status_label
    description: Metadata column status_label from source table.
  - name: source_dataset_id
    description: Metadata column source_dataset_id from source table.
  - name: linear_index
    description: Metadata column linear_index from source table.
- name: fct_hlth_ehis_de3
  description: Fact view for source table 'hlth_ehis_de3'. Contains measures and foreign
    keys to conformed dimensions.
  columns:
  - name: geo_code
    description: Foreign key to dim_geo.geo_code, linking to the geo dimension.
    tests:
    - relationships:
        to: dim_geo
        field: geo_code
  - name: sex_code
    description: Foreign key to dim_sex.sex_code, linking to the sex dimension.
    tests:
    - relationships:
        to: dim_sex
        field: sex_code
  - name: age_code
    description: Foreign key to dim_age.age_code, linking to the age dimension.
    tests:
    - relationships:
        to: dim_age
        field: age_code
  - name: time_code
    description: Foreign key to dim_time.time_code, linking to the time dimension.
    tests:
    - relationships:
        to: dim_time
        field: time_code
  - name: freq_code
    description: Foreign key to dim_freq.freq_code, linking to the freq dimension.
    tests:
    - relationships:
        to: dim_freq
        field: freq_code
  - name: resid_code
    description: Foreign key to dim_resid.resid_code, linking to the resid dimension.
    tests:
    - relationships:
        to: dim_resid
        field: resid_code
  - name: unit_code
    description: Foreign key to dim_unit.unit_code, linking to the unit dimension.
    tests:
    - relationships:
        to: dim_unit
        field: unit_code
  - name: icd10_code
    description: Foreign key to dim_icd10.icd10_code, linking to the icd10 dimension.
    tests:
    - relationships:
        to: dim_icd10
        field: icd10_code
  - name: isced11_code
    description: Foreign key to dim_isced11.isced11_code, linking to the isced11 dimension.
    tests:
    - relationships:
        to: dim_isced11
        field: isced11_code
  - name: hlth_pb_code
    description: Foreign key to dim_hlth_pb.hlth_pb_code, linking to the hlth_pb dimension.
    tests:
    - relationships:
        to: dim_hlth_pb
        field: hlth_pb_code
  - name: lev_limit_code
    description: Foreign key to dim_lev_limit.lev_limit_code, linking to the lev_limit
      dimension.
    tests:
    - relationships:
        to: dim_lev_limit
        field: lev_limit_code
  - name: deg_urb_code
    description: Foreign key to dim_deg_urb.deg_urb_code, linking to the deg_urb dimension.
    tests:
    - relationships:
        to: dim_deg_urb
        field: deg_urb_code
  - name: assist_code
    description: Foreign key to dim_assist.assist_code, linking to the assist dimension.
    tests:
    - relationships:
        to: dim_assist
        field: assist_code
  - name: smoking_code
    description: Foreign key to dim_smoking.smoking_code, linking to the smoking dimension.
    tests:
    - relationships:
        to: dim_smoking
        field: smoking_code
  - name: quant_inc_code
    description: Foreign key to dim_quant_inc.quant_inc_code, linking to the quant_inc
      dimension.
    tests:
    - relationships:
        to: dim_quant_inc
        field: quant_inc_code
  - name: n_portion_code
    description: Foreign key to dim_n_portion.n_portion_code, linking to the n_portion
      dimension.
    tests:
    - relationships:
        to: dim_n_portion
        field: n_portion_code
  - name: isced97_code
    description: Foreign key to dim_isced97.isced97_code, linking to the isced97 dimension.
    tests:
    - relationships:
        to: dim_isced97
        field: isced97_code
  - name: quantile_code
    description: Foreign key to dim_quantile.quantile_code, linking to the quantile
      dimension.
    tests:
    - relationships:
        to: dim_quantile
        field: quantile_code
  - name: bmi_code
    description: Foreign key to dim_bmi.bmi_code, linking to the bmi dimension.
    tests:
    - relationships:
        to: dim_bmi
        field: bmi_code
  - name: lev_diff_code
    description: Foreign key to dim_lev_diff.lev_diff_code, linking to the lev_diff
      dimension.
    tests:
    - relationships:
        to: dim_lev_diff
        field: lev_diff_code
  - name: yes_no_code
    description: Foreign key to dim_yes_no.yes_no_code, linking to the yes_no dimension.
    tests:
    - relationships:
        to: dim_yes_no
        field: yes_no_code
  - name: value
    description: Measure value from source table.
    tests:
    - not_null
  - name: status_code
    description: Metadata column status_code from source table.
  - name: status_label
    description: Metadata column status_label from source table.
  - name: source_dataset_id
    description: Metadata column source_dataset_id from source table.
  - name: linear_index
    description: Metadata column linear_index from source table.
- name: fct_hlth_ehis_hahlpu
  description: Fact view for source table 'hlth_ehis_hahlpu'. Contains measures and
    foreign keys to conformed dimensions.
  columns:
  - name: geo_code
    description: Foreign key to dim_geo.geo_code, linking to the geo dimension.
    tests:
    - relationships:
        to: dim_geo
        field: geo_code
  - name: sex_code
    description: Foreign key to dim_sex.sex_code, linking to the sex dimension.
    tests:
    - relationships:
        to: dim_sex
        field: sex_code
  - name: age_code
    description: Foreign key to dim_age.age_code, linking to the age dimension.
    tests:
    - relationships:
        to: dim_age
        field: age_code
  - name: time_code
    description: Foreign key to dim_time.time_code, linking to the time dimension.
    tests:
    - relationships:
        to: dim_time
        field: time_code
  - name: freq_code
    description: Foreign key to dim_freq.freq_code, linking to the freq dimension.
    tests:
    - relationships:
        to: dim_freq
        field: freq_code
  - name: resid_code
    description: Foreign key to dim_resid.resid_code, linking to the resid dimension.
    tests:
    - relationships:
        to: dim_resid
        field: resid_code
  - name: unit_code
    description: Foreign key to dim_unit.unit_code, linking to the unit dimension.
    tests:
    - relationships:
        to: dim_unit
        field: unit_code
  - name: icd10_code
    description: Foreign key to dim_icd10.icd10_code, linking to the icd10 dimension.
    tests:
    - relationships:
        to: dim_icd10
        field: icd10_code
  - name: isced11_code
    description: Foreign key to dim_isced11.isced11_code, linking to the isced11 dimension.
    tests:
    - relationships:
        to: dim_isced11
        field: isced11_code
  - name: hlth_pb_code
    description: Foreign key to dim_hlth_pb.hlth_pb_code, linking to the hlth_pb dimension.
    tests:
    - relationships:
        to: dim_hlth_pb
        field: hlth_pb_code
  - name: lev_limit_code
    description: Foreign key to dim_lev_limit.lev_limit_code, linking to the lev_limit
      dimension.
    tests:
    - relationships:
        to: dim_lev_limit
        field: lev_limit_code
  - name: deg_urb_code
    description: Foreign key to dim_deg_urb.deg_urb_code, linking to the deg_urb dimension.
    tests:
    - relationships:
        to: dim_deg_urb
        field: deg_urb_code
  - name: assist_code
    description: Foreign key to dim_assist.assist_code, linking to the assist dimension.
    tests:
    - relationships:
        to: dim_assist
        field: assist_code
  - name: smoking_code
    description: Foreign key to dim_smoking.smoking_code, linking to the smoking dimension.
    tests:
    - relationships:
        to: dim_smoking
        field: smoking_code
  - name: quant_inc_code
    description: Foreign key to dim_quant_inc.quant_inc_code, linking to the quant_inc
      dimension.
    tests:
    - relationships:
        to: dim_quant_inc
        field: quant_inc_code
  - name: n_portion_code
    description: Foreign key to dim_n_portion.n_portion_code, linking to the n_portion
      dimension.
    tests:
    - relationships:
        to: dim_n_portion
        field: n_portion_code
  - name: isced97_code
    description: Foreign key to dim_isced97.isced97_code, linking to the isced97 dimension.
    tests:
    - relationships:
        to: dim_isced97
        field: isced97_code
  - name: quantile_code
    description: Foreign key to dim_quantile.quantile_code, linking to the quantile
      dimension.
    tests:
    - relationships:
        to: dim_quantile
        field: quantile_code
  - name: bmi_code
    description: Foreign key to dim_bmi.bmi_code, linking to the bmi dimension.
    tests:
    - relationships:
        to: dim_bmi
        field: bmi_code
  - name: lev_diff_code
    description: Foreign key to dim_lev_diff.lev_diff_code, linking to the lev_diff
      dimension.
    tests:
    - relationships:
        to: dim_lev_diff
        field: lev_diff_code
  - name: yes_no_code
    description: Foreign key to dim_yes_no.yes_no_code, linking to the yes_no dimension.
    tests:
    - relationships:
        to: dim_yes_no
        field: yes_no_code
  - name: value
    description: Measure value from source table.
    tests:
    - not_null
  - name: status_code
    description: Metadata column status_code from source table.
  - name: status_label
    description: Metadata column status_label from source table.
  - name: source_dataset_id
    description: Metadata column source_dataset_id from source table.
  - name: linear_index
    description: Metadata column linear_index from source table.
- name: fct_hlth_ehis_fv3i
  description: Fact view for source table 'hlth_ehis_fv3i'. Contains measures and
    foreign keys to conformed dimensions.
  columns:
  - name: geo_code
    description: Foreign key to dim_geo.geo_code, linking to the geo dimension.
    tests:
    - relationships:
        to: dim_geo
        field: geo_code
  - name: sex_code
    description: Foreign key to dim_sex.sex_code, linking to the sex dimension.
    tests:
    - relationships:
        to: dim_sex
        field: sex_code
  - name: age_code
    description: Foreign key to dim_age.age_code, linking to the age dimension.
    tests:
    - relationships:
        to: dim_age
        field: age_code
  - name: time_code
    description: Foreign key to dim_time.time_code, linking to the time dimension.
    tests:
    - relationships:
        to: dim_time
        field: time_code
  - name: freq_code
    description: Foreign key to dim_freq.freq_code, linking to the freq dimension.
    tests:
    - relationships:
        to: dim_freq
        field: freq_code
  - name: resid_code
    description: Foreign key to dim_resid.resid_code, linking to the resid dimension.
    tests:
    - relationships:
        to: dim_resid
        field: resid_code
  - name: unit_code
    description: Foreign key to dim_unit.unit_code, linking to the unit dimension.
    tests:
    - relationships:
        to: dim_unit
        field: unit_code
  - name: icd10_code
    description: Foreign key to dim_icd10.icd10_code, linking to the icd10 dimension.
    tests:
    - relationships:
        to: dim_icd10
        field: icd10_code
  - name: isced11_code
    description: Foreign key to dim_isced11.isced11_code, linking to the isced11 dimension.
    tests:
    - relationships:
        to: dim_isced11
        field: isced11_code
  - name: hlth_pb_code
    description: Foreign key to dim_hlth_pb.hlth_pb_code, linking to the hlth_pb dimension.
    tests:
    - relationships:
        to: dim_hlth_pb
        field: hlth_pb_code
  - name: lev_limit_code
    description: Foreign key to dim_lev_limit.lev_limit_code, linking to the lev_limit
      dimension.
    tests:
    - relationships:
        to: dim_lev_limit
        field: lev_limit_code
  - name: deg_urb_code
    description: Foreign key to dim_deg_urb.deg_urb_code, linking to the deg_urb dimension.
    tests:
    - relationships:
        to: dim_deg_urb
        field: deg_urb_code
  - name: assist_code
    description: Foreign key to dim_assist.assist_code, linking to the assist dimension.
    tests:
    - relationships:
        to: dim_assist
        field: assist_code
  - name: smoking_code
    description: Foreign key to dim_smoking.smoking_code, linking to the smoking dimension.
    tests:
    - relationships:
        to: dim_smoking
        field: smoking_code
  - name: quant_inc_code
    description: Foreign key to dim_quant_inc.quant_inc_code, linking to the quant_inc
      dimension.
    tests:
    - relationships:
        to: dim_quant_inc
        field: quant_inc_code
  - name: n_portion_code
    description: Foreign key to dim_n_portion.n_portion_code, linking to the n_portion
      dimension.
    tests:
    - relationships:
        to: dim_n_portion
        field: n_portion_code
  - name: isced97_code
    description: Foreign key to dim_isced97.isced97_code, linking to the isced97 dimension.
    tests:
    - relationships:
        to: dim_isced97
        field: isced97_code
  - name: quantile_code
    description: Foreign key to dim_quantile.quantile_code, linking to the quantile
      dimension.
    tests:
    - relationships:
        to: dim_quantile
        field: quantile_code
  - name: bmi_code
    description: Foreign key to dim_bmi.bmi_code, linking to the bmi dimension.
    tests:
    - relationships:
        to: dim_bmi
        field: bmi_code
  - name: lev_diff_code
    description: Foreign key to dim_lev_diff.lev_diff_code, linking to the lev_diff
      dimension.
    tests:
    - relationships:
        to: dim_lev_diff
        field: lev_diff_code
  - name: yes_no_code
    description: Foreign key to dim_yes_no.yes_no_code, linking to the yes_no dimension.
    tests:
    - relationships:
        to: dim_yes_no
        field: yes_no_code
  - name: value
    description: Measure value from source table.
    tests:
    - not_null
  - name: status_code
    description: Metadata column status_code from source table.
  - name: status_label
    description: Metadata column status_label from source table.
  - name: source_dataset_id
    description: Metadata column source_dataset_id from source table.
  - name: linear_index
    description: Metadata column linear_index from source table.
- name: fct_hlth_ehis_sk3u
  description: Fact view for source table 'hlth_ehis_sk3u'. Contains measures and
    foreign keys to conformed dimensions.
  columns:
  - name: geo_code
    description: Foreign key to dim_geo.geo_code, linking to the geo dimension.
    tests:
    - relationships:
        to: dim_geo
        field: geo_code
  - name: sex_code
    description: Foreign key to dim_sex.sex_code, linking to the sex dimension.
    tests:
    - relationships:
        to: dim_sex
        field: sex_code
  - name: age_code
    description: Foreign key to dim_age.age_code, linking to the age dimension.
    tests:
    - relationships:
        to: dim_age
        field: age_code
  - name: time_code
    description: Foreign key to dim_time.time_code, linking to the time dimension.
    tests:
    - relationships:
        to: dim_time
        field: time_code
  - name: freq_code
    description: Foreign key to dim_freq.freq_code, linking to the freq dimension.
    tests:
    - relationships:
        to: dim_freq
        field: freq_code
  - name: resid_code
    description: Foreign key to dim_resid.resid_code, linking to the resid dimension.
    tests:
    - relationships:
        to: dim_resid
        field: resid_code
  - name: unit_code
    description: Foreign key to dim_unit.unit_code, linking to the unit dimension.
    tests:
    - relationships:
        to: dim_unit
        field: unit_code
  - name: icd10_code
    description: Foreign key to dim_icd10.icd10_code, linking to the icd10 dimension.
    tests:
    - relationships:
        to: dim_icd10
        field: icd10_code
  - name: isced11_code
    description: Foreign key to dim_isced11.isced11_code, linking to the isced11 dimension.
    tests:
    - relationships:
        to: dim_isced11
        field: isced11_code
  - name: hlth_pb_code
    description: Foreign key to dim_hlth_pb.hlth_pb_code, linking to the hlth_pb dimension.
    tests:
    - relationships:
        to: dim_hlth_pb
        field: hlth_pb_code
  - name: lev_limit_code
    description: Foreign key to dim_lev_limit.lev_limit_code, linking to the lev_limit
      dimension.
    tests:
    - relationships:
        to: dim_lev_limit
        field: lev_limit_code
  - name: deg_urb_code
    description: Foreign key to dim_deg_urb.deg_urb_code, linking to the deg_urb dimension.
    tests:
    - relationships:
        to: dim_deg_urb
        field: deg_urb_code
  - name: assist_code
    description: Foreign key to dim_assist.assist_code, linking to the assist dimension.
    tests:
    - relationships:
        to: dim_assist
        field: assist_code
  - name: smoking_code
    description: Foreign key to dim_smoking.smoking_code, linking to the smoking dimension.
    tests:
    - relationships:
        to: dim_smoking
        field: smoking_code
  - name: quant_inc_code
    description: Foreign key to dim_quant_inc.quant_inc_code, linking to the quant_inc
      dimension.
    tests:
    - relationships:
        to: dim_quant_inc
        field: quant_inc_code
  - name: n_portion_code
    description: Foreign key to dim_n_portion.n_portion_code, linking to the n_portion
      dimension.
    tests:
    - relationships:
        to: dim_n_portion
        field: n_portion_code
  - name: isced97_code
    description: Foreign key to dim_isced97.isced97_code, linking to the isced97 dimension.
    tests:
    - relationships:
        to: dim_isced97
        field: isced97_code
  - name: quantile_code
    description: Foreign key to dim_quantile.quantile_code, linking to the quantile
      dimension.
    tests:
    - relationships:
        to: dim_quantile
        field: quantile_code
  - name: bmi_code
    description: Foreign key to dim_bmi.bmi_code, linking to the bmi dimension.
    tests:
    - relationships:
        to: dim_bmi
        field: bmi_code
  - name: lev_diff_code
    description: Foreign key to dim_lev_diff.lev_diff_code, linking to the lev_diff
      dimension.
    tests:
    - relationships:
        to: dim_lev_diff
        field: lev_diff_code
  - name: yes_no_code
    description: Foreign key to dim_yes_no.yes_no_code, linking to the yes_no dimension.
    tests:
    - relationships:
        to: dim_yes_no
        field: yes_no_code
  - name: value
    description: Measure value from source table.
    tests:
    - not_null
  - name: status_code
    description: Metadata column status_code from source table.
  - name: status_label
    description: Metadata column status_label from source table.
  - name: source_dataset_id
    description: Metadata column source_dataset_id from source table.
  - name: linear_index
    description: Metadata column linear_index from source table.
- name: fct_hlth_ehis_de4
  description: Fact view for source table 'hlth_ehis_de4'. Contains measures and foreign
    keys to conformed dimensions.
  columns:
  - name: geo_code
    description: Foreign key to dim_geo.geo_code, linking to the geo dimension.
    tests:
    - relationships:
        to: dim_geo
        field: geo_code
  - name: sex_code
    description: Foreign key to dim_sex.sex_code, linking to the sex dimension.
    tests:
    - relationships:
        to: dim_sex
        field: sex_code
  - name: age_code
    description: Foreign key to dim_age.age_code, linking to the age dimension.
    tests:
    - relationships:
        to: dim_age
        field: age_code
  - name: time_code
    description: Foreign key to dim_time.time_code, linking to the time dimension.
    tests:
    - relationships:
        to: dim_time
        field: time_code
  - name: freq_code
    description: Foreign key to dim_freq.freq_code, linking to the freq dimension.
    tests:
    - relationships:
        to: dim_freq
        field: freq_code
  - name: resid_code
    description: Foreign key to dim_resid.resid_code, linking to the resid dimension.
    tests:
    - relationships:
        to: dim_resid
        field: resid_code
  - name: unit_code
    description: Foreign key to dim_unit.unit_code, linking to the unit dimension.
    tests:
    - relationships:
        to: dim_unit
        field: unit_code
  - name: icd10_code
    description: Foreign key to dim_icd10.icd10_code, linking to the icd10 dimension.
    tests:
    - relationships:
        to: dim_icd10
        field: icd10_code
  - name: isced11_code
    description: Foreign key to dim_isced11.isced11_code, linking to the isced11 dimension.
    tests:
    - relationships:
        to: dim_isced11
        field: isced11_code
  - name: hlth_pb_code
    description: Foreign key to dim_hlth_pb.hlth_pb_code, linking to the hlth_pb dimension.
    tests:
    - relationships:
        to: dim_hlth_pb
        field: hlth_pb_code
  - name: lev_limit_code
    description: Foreign key to dim_lev_limit.lev_limit_code, linking to the lev_limit
      dimension.
    tests:
    - relationships:
        to: dim_lev_limit
        field: lev_limit_code
  - name: deg_urb_code
    description: Foreign key to dim_deg_urb.deg_urb_code, linking to the deg_urb dimension.
    tests:
    - relationships:
        to: dim_deg_urb
        field: deg_urb_code
  - name: assist_code
    description: Foreign key to dim_assist.assist_code, linking to the assist dimension.
    tests:
    - relationships:
        to: dim_assist
        field: assist_code
  - name: smoking_code
    description: Foreign key to dim_smoking.smoking_code, linking to the smoking dimension.
    tests:
    - relationships:
        to: dim_smoking
        field: smoking_code
  - name: quant_inc_code
    description: Foreign key to dim_quant_inc.quant_inc_code, linking to the quant_inc
      dimension.
    tests:
    - relationships:
        to: dim_quant_inc
        field: quant_inc_code
  - name: n_portion_code
    description: Foreign key to dim_n_portion.n_portion_code, linking to the n_portion
      dimension.
    tests:
    - relationships:
        to: dim_n_portion
        field: n_portion_code
  - name: isced97_code
    description: Foreign key to dim_isced97.isced97_code, linking to the isced97 dimension.
    tests:
    - relationships:
        to: dim_isced97
        field: isced97_code
  - name: quantile_code
    description: Foreign key to dim_quantile.quantile_code, linking to the quantile
      dimension.
    tests:
    - relationships:
        to: dim_quantile
        field: quantile_code
  - name: bmi_code
    description: Foreign key to dim_bmi.bmi_code, linking to the bmi dimension.
    tests:
    - relationships:
        to: dim_bmi
        field: bmi_code
  - name: lev_diff_code
    description: Foreign key to dim_lev_diff.lev_diff_code, linking to the lev_diff
      dimension.
    tests:
    - relationships:
        to: dim_lev_diff
        field: lev_diff_code
  - name: yes_no_code
    description: Foreign key to dim_yes_no.yes_no_code, linking to the yes_no dimension.
    tests:
    - relationships:
        to: dim_yes_no
        field: yes_no_code
  - name: value
    description: Measure value from source table.
    tests:
    - not_null
  - name: status_code
    description: Metadata column status_code from source table.
  - name: status_label
    description: Metadata column status_label from source table.
  - name: source_dataset_id
    description: Metadata column source_dataset_id from source table.
  - name: linear_index
    description: Metadata column linear_index from source table.
- name: fct_hlth_ehis_pc1i
  description: Fact view for source table 'hlth_ehis_pc1i'. Contains measures and
    foreign keys to conformed dimensions.
  columns:
  - name: geo_code
    description: Foreign key to dim_geo.geo_code, linking to the geo dimension.
    tests:
    - relationships:
        to: dim_geo
        field: geo_code
  - name: sex_code
    description: Foreign key to dim_sex.sex_code, linking to the sex dimension.
    tests:
    - relationships:
        to: dim_sex
        field: sex_code
  - name: age_code
    description: Foreign key to dim_age.age_code, linking to the age dimension.
    tests:
    - relationships:
        to: dim_age
        field: age_code
  - name: time_code
    description: Foreign key to dim_time.time_code, linking to the time dimension.
    tests:
    - relationships:
        to: dim_time
        field: time_code
  - name: freq_code
    description: Foreign key to dim_freq.freq_code, linking to the freq dimension.
    tests:
    - relationships:
        to: dim_freq
        field: freq_code
  - name: resid_code
    description: Foreign key to dim_resid.resid_code, linking to the resid dimension.
    tests:
    - relationships:
        to: dim_resid
        field: resid_code
  - name: unit_code
    description: Foreign key to dim_unit.unit_code, linking to the unit dimension.
    tests:
    - relationships:
        to: dim_unit
        field: unit_code
  - name: icd10_code
    description: Foreign key to dim_icd10.icd10_code, linking to the icd10 dimension.
    tests:
    - relationships:
        to: dim_icd10
        field: icd10_code
  - name: isced11_code
    description: Foreign key to dim_isced11.isced11_code, linking to the isced11 dimension.
    tests:
    - relationships:
        to: dim_isced11
        field: isced11_code
  - name: hlth_pb_code
    description: Foreign key to dim_hlth_pb.hlth_pb_code, linking to the hlth_pb dimension.
    tests:
    - relationships:
        to: dim_hlth_pb
        field: hlth_pb_code
  - name: lev_limit_code
    description: Foreign key to dim_lev_limit.lev_limit_code, linking to the lev_limit
      dimension.
    tests:
    - relationships:
        to: dim_lev_limit
        field: lev_limit_code
  - name: deg_urb_code
    description: Foreign key to dim_deg_urb.deg_urb_code, linking to the deg_urb dimension.
    tests:
    - relationships:
        to: dim_deg_urb
        field: deg_urb_code
  - name: assist_code
    description: Foreign key to dim_assist.assist_code, linking to the assist dimension.
    tests:
    - relationships:
        to: dim_assist
        field: assist_code
  - name: smoking_code
    description: Foreign key to dim_smoking.smoking_code, linking to the smoking dimension.
    tests:
    - relationships:
        to: dim_smoking
        field: smoking_code
  - name: quant_inc_code
    description: Foreign key to dim_quant_inc.quant_inc_code, linking to the quant_inc
      dimension.
    tests:
    - relationships:
        to: dim_quant_inc
        field: quant_inc_code
  - name: n_portion_code
    description: Foreign key to dim_n_portion.n_portion_code, linking to the n_portion
      dimension.
    tests:
    - relationships:
        to: dim_n_portion
        field: n_portion_code
  - name: isced97_code
    description: Foreign key to dim_isced97.isced97_code, linking to the isced97 dimension.
    tests:
    - relationships:
        to: dim_isced97
        field: isced97_code
  - name: quantile_code
    description: Foreign key to dim_quantile.quantile_code, linking to the quantile
      dimension.
    tests:
    - relationships:
        to: dim_quantile
        field: quantile_code
  - name: bmi_code
    description: Foreign key to dim_bmi.bmi_code, linking to the bmi dimension.
    tests:
    - relationships:
        to: dim_bmi
        field: bmi_code
  - name: lev_diff_code
    description: Foreign key to dim_lev_diff.lev_diff_code, linking to the lev_diff
      dimension.
    tests:
    - relationships:
        to: dim_lev_diff
        field: lev_diff_code
  - name: yes_no_code
    description: Foreign key to dim_yes_no.yes_no_code, linking to the yes_no dimension.
    tests:
    - relationships:
        to: dim_yes_no
        field: yes_no_code
  - name: value
    description: Measure value from source table.
    tests:
    - not_null
  - name: status_code
    description: Metadata column status_code from source table.
  - name: status_label
    description: Metadata column status_label from source table.
  - name: source_dataset_id
    description: Metadata column source_dataset_id from source table.
  - name: linear_index
    description: Metadata column linear_index from source table.
- name: fct_hlth_ehis_fv3m
  description: Fact view for source table 'hlth_ehis_fv3m'. Contains measures and
    foreign keys to conformed dimensions.
  columns:
  - name: geo_code
    description: Foreign key to dim_geo.geo_code, linking to the geo dimension.
    tests:
    - relationships:
        to: dim_geo
        field: geo_code
  - name: sex_code
    description: Foreign key to dim_sex.sex_code, linking to the sex dimension.
    tests:
    - relationships:
        to: dim_sex
        field: sex_code
  - name: age_code
    description: Foreign key to dim_age.age_code, linking to the age dimension.
    tests:
    - relationships:
        to: dim_age
        field: age_code
  - name: time_code
    description: Foreign key to dim_time.time_code, linking to the time dimension.
    tests:
    - relationships:
        to: dim_time
        field: time_code
  - name: freq_code
    description: Foreign key to dim_freq.freq_code, linking to the freq dimension.
    tests:
    - relationships:
        to: dim_freq
        field: freq_code
  - name: resid_code
    description: Foreign key to dim_resid.resid_code, linking to the resid dimension.
    tests:
    - relationships:
        to: dim_resid
        field: resid_code
  - name: unit_code
    description: Foreign key to dim_unit.unit_code, linking to the unit dimension.
    tests:
    - relationships:
        to: dim_unit
        field: unit_code
  - name: icd10_code
    description: Foreign key to dim_icd10.icd10_code, linking to the icd10 dimension.
    tests:
    - relationships:
        to: dim_icd10
        field: icd10_code
  - name: isced11_code
    description: Foreign key to dim_isced11.isced11_code, linking to the isced11 dimension.
    tests:
    - relationships:
        to: dim_isced11
        field: isced11_code
  - name: hlth_pb_code
    description: Foreign key to dim_hlth_pb.hlth_pb_code, linking to the hlth_pb dimension.
    tests:
    - relationships:
        to: dim_hlth_pb
        field: hlth_pb_code
  - name: lev_limit_code
    description: Foreign key to dim_lev_limit.lev_limit_code, linking to the lev_limit
      dimension.
    tests:
    - relationships:
        to: dim_lev_limit
        field: lev_limit_code
  - name: deg_urb_code
    description: Foreign key to dim_deg_urb.deg_urb_code, linking to the deg_urb dimension.
    tests:
    - relationships:
        to: dim_deg_urb
        field: deg_urb_code
  - name: assist_code
    description: Foreign key to dim_assist.assist_code, linking to the assist dimension.
    tests:
    - relationships:
        to: dim_assist
        field: assist_code
  - name: smoking_code
    description: Foreign key to dim_smoking.smoking_code, linking to the smoking dimension.
    tests:
    - relationships:
        to: dim_smoking
        field: smoking_code
  - name: quant_inc_code
    description: Foreign key to dim_quant_inc.quant_inc_code, linking to the quant_inc
      dimension.
    tests:
    - relationships:
        to: dim_quant_inc
        field: quant_inc_code
  - name: n_portion_code
    description: Foreign key to dim_n_portion.n_portion_code, linking to the n_portion
      dimension.
    tests:
    - relationships:
        to: dim_n_portion
        field: n_portion_code
  - name: isced97_code
    description: Foreign key to dim_isced97.isced97_code, linking to the isced97 dimension.
    tests:
    - relationships:
        to: dim_isced97
        field: isced97_code
  - name: quantile_code
    description: Foreign key to dim_quantile.quantile_code, linking to the quantile
      dimension.
    tests:
    - relationships:
        to: dim_quantile
        field: quantile_code
  - name: bmi_code
    description: Foreign key to dim_bmi.bmi_code, linking to the bmi dimension.
    tests:
    - relationships:
        to: dim_bmi
        field: bmi_code
  - name: lev_diff_code
    description: Foreign key to dim_lev_diff.lev_diff_code, linking to the lev_diff
      dimension.
    tests:
    - relationships:
        to: dim_lev_diff
        field: lev_diff_code
  - name: yes_no_code
    description: Foreign key to dim_yes_no.yes_no_code, linking to the yes_no dimension.
    tests:
    - relationships:
        to: dim_yes_no
        field: yes_no_code
  - name: value
    description: Measure value from source table.
    tests:
    - not_null
  - name: status_code
    description: Metadata column status_code from source table.
  - name: status_label
    description: Metadata column status_label from source table.
  - name: source_dataset_id
    description: Metadata column source_dataset_id from source table.
  - name: linear_index
    description: Metadata column linear_index from source table.
- name: fct_hlth_ehis_fv3u
  description: Fact view for source table 'hlth_ehis_fv3u'. Contains measures and
    foreign keys to conformed dimensions.
  columns:
  - name: geo_code
    description: Foreign key to dim_geo.geo_code, linking to the geo dimension.
    tests:
    - relationships:
        to: dim_geo
        field: geo_code
  - name: sex_code
    description: Foreign key to dim_sex.sex_code, linking to the sex dimension.
    tests:
    - relationships:
        to: dim_sex
        field: sex_code
  - name: age_code
    description: Foreign key to dim_age.age_code, linking to the age dimension.
    tests:
    - relationships:
        to: dim_age
        field: age_code
  - name: time_code
    description: Foreign key to dim_time.time_code, linking to the time dimension.
    tests:
    - relationships:
        to: dim_time
        field: time_code
  - name: freq_code
    description: Foreign key to dim_freq.freq_code, linking to the freq dimension.
    tests:
    - relationships:
        to: dim_freq
        field: freq_code
  - name: resid_code
    description: Foreign key to dim_resid.resid_code, linking to the resid dimension.
    tests:
    - relationships:
        to: dim_resid
        field: resid_code
  - name: unit_code
    description: Foreign key to dim_unit.unit_code, linking to the unit dimension.
    tests:
    - relationships:
        to: dim_unit
        field: unit_code
  - name: icd10_code
    description: Foreign key to dim_icd10.icd10_code, linking to the icd10 dimension.
    tests:
    - relationships:
        to: dim_icd10
        field: icd10_code
  - name: isced11_code
    description: Foreign key to dim_isced11.isced11_code, linking to the isced11 dimension.
    tests:
    - relationships:
        to: dim_isced11
        field: isced11_code
  - name: hlth_pb_code
    description: Foreign key to dim_hlth_pb.hlth_pb_code, linking to the hlth_pb dimension.
    tests:
    - relationships:
        to: dim_hlth_pb
        field: hlth_pb_code
  - name: lev_limit_code
    description: Foreign key to dim_lev_limit.lev_limit_code, linking to the lev_limit
      dimension.
    tests:
    - relationships:
        to: dim_lev_limit
        field: lev_limit_code
  - name: deg_urb_code
    description: Foreign key to dim_deg_urb.deg_urb_code, linking to the deg_urb dimension.
    tests:
    - relationships:
        to: dim_deg_urb
        field: deg_urb_code
  - name: assist_code
    description: Foreign key to dim_assist.assist_code, linking to the assist dimension.
    tests:
    - relationships:
        to: dim_assist
        field: assist_code
  - name: smoking_code
    description: Foreign key to dim_smoking.smoking_code, linking to the smoking dimension.
    tests:
    - relationships:
        to: dim_smoking
        field: smoking_code
  - name: quant_inc_code
    description: Foreign key to dim_quant_inc.quant_inc_code, linking to the quant_inc
      dimension.
    tests:
    - relationships:
        to: dim_quant_inc
        field: quant_inc_code
  - name: n_portion_code
    description: Foreign key to dim_n_portion.n_portion_code, linking to the n_portion
      dimension.
    tests:
    - relationships:
        to: dim_n_portion
        field: n_portion_code
  - name: isced97_code
    description: Foreign key to dim_isced97.isced97_code, linking to the isced97 dimension.
    tests:
    - relationships:
        to: dim_isced97
        field: isced97_code
  - name: quantile_code
    description: Foreign key to dim_quantile.quantile_code, linking to the quantile
      dimension.
    tests:
    - relationships:
        to: dim_quantile
        field: quantile_code
  - name: bmi_code
    description: Foreign key to dim_bmi.bmi_code, linking to the bmi dimension.
    tests:
    - relationships:
        to: dim_bmi
        field: bmi_code
  - name: lev_diff_code
    description: Foreign key to dim_lev_diff.lev_diff_code, linking to the lev_diff
      dimension.
    tests:
    - relationships:
        to: dim_lev_diff
        field: lev_diff_code
  - name: yes_no_code
    description: Foreign key to dim_yes_no.yes_no_code, linking to the yes_no dimension.
    tests:
    - relationships:
        to: dim_yes_no
        field: yes_no_code
  - name: value
    description: Measure value from source table.
    tests:
    - not_null
  - name: status_code
    description: Metadata column status_code from source table.
  - name: status_label
    description: Metadata column status_label from source table.
  - name: source_dataset_id
    description: Metadata column source_dataset_id from source table.
  - name: linear_index
    description: Metadata column linear_index from source table.
- name: fct_hlth_ehis_pc1d
  description: Fact view for source table 'hlth_ehis_pc1d'. Contains measures and
    foreign keys to conformed dimensions.
  columns:
  - name: geo_code
    description: Foreign key to dim_geo.geo_code, linking to the geo dimension.
    tests:
    - relationships:
        to: dim_geo
        field: geo_code
  - name: sex_code
    description: Foreign key to dim_sex.sex_code, linking to the sex dimension.
    tests:
    - relationships:
        to: dim_sex
        field: sex_code
  - name: age_code
    description: Foreign key to dim_age.age_code, linking to the age dimension.
    tests:
    - relationships:
        to: dim_age
        field: age_code
  - name: time_code
    description: Foreign key to dim_time.time_code, linking to the time dimension.
    tests:
    - relationships:
        to: dim_time
        field: time_code
  - name: freq_code
    description: Foreign key to dim_freq.freq_code, linking to the freq dimension.
    tests:
    - relationships:
        to: dim_freq
        field: freq_code
  - name: resid_code
    description: Foreign key to dim_resid.resid_code, linking to the resid dimension.
    tests:
    - relationships:
        to: dim_resid
        field: resid_code
  - name: unit_code
    description: Foreign key to dim_unit.unit_code, linking to the unit dimension.
    tests:
    - relationships:
        to: dim_unit
        field: unit_code
  - name: icd10_code
    description: Foreign key to dim_icd10.icd10_code, linking to the icd10 dimension.
    tests:
    - relationships:
        to: dim_icd10
        field: icd10_code
  - name: isced11_code
    description: Foreign key to dim_isced11.isced11_code, linking to the isced11 dimension.
    tests:
    - relationships:
        to: dim_isced11
        field: isced11_code
  - name: hlth_pb_code
    description: Foreign key to dim_hlth_pb.hlth_pb_code, linking to the hlth_pb dimension.
    tests:
    - relationships:
        to: dim_hlth_pb
        field: hlth_pb_code
  - name: lev_limit_code
    description: Foreign key to dim_lev_limit.lev_limit_code, linking to the lev_limit
      dimension.
    tests:
    - relationships:
        to: dim_lev_limit
        field: lev_limit_code
  - name: deg_urb_code
    description: Foreign key to dim_deg_urb.deg_urb_code, linking to the deg_urb dimension.
    tests:
    - relationships:
        to: dim_deg_urb
        field: deg_urb_code
  - name: assist_code
    description: Foreign key to dim_assist.assist_code, linking to the assist dimension.
    tests:
    - relationships:
        to: dim_assist
        field: assist_code
  - name: smoking_code
    description: Foreign key to dim_smoking.smoking_code, linking to the smoking dimension.
    tests:
    - relationships:
        to: dim_smoking
        field: smoking_code
  - name: quant_inc_code
    description: Foreign key to dim_quant_inc.quant_inc_code, linking to the quant_inc
      dimension.
    tests:
    - relationships:
        to: dim_quant_inc
        field: quant_inc_code
  - name: n_portion_code
    description: Foreign key to dim_n_portion.n_portion_code, linking to the n_portion
      dimension.
    tests:
    - relationships:
        to: dim_n_portion
        field: n_portion_code
  - name: isced97_code
    description: Foreign key to dim_isced97.isced97_code, linking to the isced97 dimension.
    tests:
    - relationships:
        to: dim_isced97
        field: isced97_code
  - name: quantile_code
    description: Foreign key to dim_quantile.quantile_code, linking to the quantile
      dimension.
    tests:
    - relationships:
        to: dim_quantile
        field: quantile_code
  - name: bmi_code
    description: Foreign key to dim_bmi.bmi_code, linking to the bmi dimension.
    tests:
    - relationships:
        to: dim_bmi
        field: bmi_code
  - name: lev_diff_code
    description: Foreign key to dim_lev_diff.lev_diff_code, linking to the lev_diff
      dimension.
    tests:
    - relationships:
        to: dim_lev_diff
        field: lev_diff_code
  - name: yes_no_code
    description: Foreign key to dim_yes_no.yes_no_code, linking to the yes_no dimension.
    tests:
    - relationships:
        to: dim_yes_no
        field: yes_no_code
  - name: value
    description: Measure value from source table.
    tests:
    - not_null
  - name: status_code
    description: Metadata column status_code from source table.
  - name: status_label
    description: Metadata column status_label from source table.
  - name: source_dataset_id
    description: Metadata column source_dataset_id from source table.
  - name: linear_index
    description: Metadata column linear_index from source table.
- name: fct_hlth_ehis_pc1hcs
  description: Fact view for source table 'hlth_ehis_pc1hcs'. Contains measures and
    foreign keys to conformed dimensions.
  columns:
  - name: geo_code
    description: Foreign key to dim_geo.geo_code, linking to the geo dimension.
    tests:
    - relationships:
        to: dim_geo
        field: geo_code
  - name: sex_code
    description: Foreign key to dim_sex.sex_code, linking to the sex dimension.
    tests:
    - relationships:
        to: dim_sex
        field: sex_code
  - name: age_code
    description: Foreign key to dim_age.age_code, linking to the age dimension.
    tests:
    - relationships:
        to: dim_age
        field: age_code
  - name: time_code
    description: Foreign key to dim_time.time_code, linking to the time dimension.
    tests:
    - relationships:
        to: dim_time
        field: time_code
  - name: freq_code
    description: Foreign key to dim_freq.freq_code, linking to the freq dimension.
    tests:
    - relationships:
        to: dim_freq
        field: freq_code
  - name: resid_code
    description: Foreign key to dim_resid.resid_code, linking to the resid dimension.
    tests:
    - relationships:
        to: dim_resid
        field: resid_code
  - name: unit_code
    description: Foreign key to dim_unit.unit_code, linking to the unit dimension.
    tests:
    - relationships:
        to: dim_unit
        field: unit_code
  - name: icd10_code
    description: Foreign key to dim_icd10.icd10_code, linking to the icd10 dimension.
    tests:
    - relationships:
        to: dim_icd10
        field: icd10_code
  - name: isced11_code
    description: Foreign key to dim_isced11.isced11_code, linking to the isced11 dimension.
    tests:
    - relationships:
        to: dim_isced11
        field: isced11_code
  - name: hlth_pb_code
    description: Foreign key to dim_hlth_pb.hlth_pb_code, linking to the hlth_pb dimension.
    tests:
    - relationships:
        to: dim_hlth_pb
        field: hlth_pb_code
  - name: lev_limit_code
    description: Foreign key to dim_lev_limit.lev_limit_code, linking to the lev_limit
      dimension.
    tests:
    - relationships:
        to: dim_lev_limit
        field: lev_limit_code
  - name: deg_urb_code
    description: Foreign key to dim_deg_urb.deg_urb_code, linking to the deg_urb dimension.
    tests:
    - relationships:
        to: dim_deg_urb
        field: deg_urb_code
  - name: assist_code
    description: Foreign key to dim_assist.assist_code, linking to the assist dimension.
    tests:
    - relationships:
        to: dim_assist
        field: assist_code
  - name: smoking_code
    description: Foreign key to dim_smoking.smoking_code, linking to the smoking dimension.
    tests:
    - relationships:
        to: dim_smoking
        field: smoking_code
  - name: quant_inc_code
    description: Foreign key to dim_quant_inc.quant_inc_code, linking to the quant_inc
      dimension.
    tests:
    - relationships:
        to: dim_quant_inc
        field: quant_inc_code
  - name: n_portion_code
    description: Foreign key to dim_n_portion.n_portion_code, linking to the n_portion
      dimension.
    tests:
    - relationships:
        to: dim_n_portion
        field: n_portion_code
  - name: isced97_code
    description: Foreign key to dim_isced97.isced97_code, linking to the isced97 dimension.
    tests:
    - relationships:
        to: dim_isced97
        field: isced97_code
  - name: quantile_code
    description: Foreign key to dim_quantile.quantile_code, linking to the quantile
      dimension.
    tests:
    - relationships:
        to: dim_quantile
        field: quantile_code
  - name: bmi_code
    description: Foreign key to dim_bmi.bmi_code, linking to the bmi dimension.
    tests:
    - relationships:
        to: dim_bmi
        field: bmi_code
  - name: lev_diff_code
    description: Foreign key to dim_lev_diff.lev_diff_code, linking to the lev_diff
      dimension.
    tests:
    - relationships:
        to: dim_lev_diff
        field: lev_diff_code
  - name: yes_no_code
    description: Foreign key to dim_yes_no.yes_no_code, linking to the yes_no dimension.
    tests:
    - relationships:
        to: dim_yes_no
        field: yes_no_code
  - name: value
    description: Measure value from source table.
    tests:
    - not_null
  - name: status_code
    description: Metadata column status_code from source table.
  - name: status_label
    description: Metadata column status_label from source table.
  - name: source_dataset_id
    description: Metadata column source_dataset_id from source table.
  - name: linear_index
    description: Metadata column linear_index from source table.
